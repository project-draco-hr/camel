{
  ZooKeeper connection=getConnection();
  AnyOfOperations operation=new AnyOfOperations(node,new ExistsOperation(connection,node),new ExistenceChangedOperation(connection,node));
  return operation;
}
