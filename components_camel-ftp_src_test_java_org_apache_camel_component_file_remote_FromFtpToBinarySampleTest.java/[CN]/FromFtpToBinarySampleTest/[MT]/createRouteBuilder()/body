{
  return new RouteBuilder(){
    public void configure() throws Exception {
      long delay=60 * 60 * 1000L;
      from("ftp://scott@localhost/public/reports?password=tiger&binary=true&consumer.delay=" + delay).to("file://target/test-reports");
    }
  }
;
}
