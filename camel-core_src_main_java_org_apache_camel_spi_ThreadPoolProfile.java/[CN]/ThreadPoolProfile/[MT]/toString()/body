{
  return "ThreadPoolProfile[" + id + " ("+ defaultProfile+ ") size:"+ poolSize+ "-"+ maxPoolSize+ ", keepAlive: "+ keepAliveTime+ " "+ timeUnit+ ", maxQueue: "+ maxQueueSize+ ", rejectedPolicy:"+ rejectedPolicy+ "]";
}
