{
  String val=row.get(key);
  if (val == null) {
    val="";
  }
  String outStr=String.format(formatString(),val);
  if (newLine) {
    out.println(outStr.trim());
  }
 else {
    out.print(outStr);
    out.print(' ');
  }
}
