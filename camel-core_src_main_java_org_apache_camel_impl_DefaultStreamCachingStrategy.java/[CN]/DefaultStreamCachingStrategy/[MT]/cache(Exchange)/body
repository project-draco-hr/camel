{
  StreamCache cache=exchange.getIn().getBody(StreamCache.class);
  if (cache != null) {
    if (cache.inMemory()) {
      cacheMemoryCounter++;
    }
 else {
      cacheSpoolCounter++;
    }
  }
  return cache;
}
