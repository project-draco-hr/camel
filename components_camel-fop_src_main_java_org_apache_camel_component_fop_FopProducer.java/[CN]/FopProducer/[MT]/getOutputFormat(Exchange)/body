{
  String outputFormat=exchange.getIn().getHeader(FopConstants.CAMEL_FOP_OUTPUT_FORMAT,this.outputFormat,String.class);
  if (outputFormat == null) {
    throw new RuntimeExchangeException("Missing output format",exchange);
  }
  return outputFormat;
}
