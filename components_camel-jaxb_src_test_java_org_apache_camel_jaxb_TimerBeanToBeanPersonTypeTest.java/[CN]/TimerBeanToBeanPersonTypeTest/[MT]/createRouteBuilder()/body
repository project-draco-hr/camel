{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("timer:foo?repeatCount=1").log("Timer triggered").bean(MyPersonService.class,"createPerson").bean(MyPersonService.class,"sendPerson");
    }
  }
;
}
