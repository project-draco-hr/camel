{
  SpringBatchEndpoint endpoint=new SpringBatchEndpoint(uri,this,jobLauncher,defaultResolvedJobLauncher,allResolvedJobLaunchers,remaining,jobRegistry);
  setProperties(endpoint,parameters);
  return endpoint;
}
