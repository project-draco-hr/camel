{
  String answer=null;
  if (delegate != null) {
    answer=delegate.parseProperty(key,value,properties);
  }
  if (answer != null) {
    value=answer;
  }
  return parser.parseProperty(key,value,properties);
}
