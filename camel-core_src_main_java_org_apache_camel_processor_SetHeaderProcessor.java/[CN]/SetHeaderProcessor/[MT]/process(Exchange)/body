{
  Object value=expression.evaluate(exchange);
  if (value == null) {
    LOG.warn("Expression: " + expression + " on exchange: "+ exchange+ " evaluated to null.");
  }
  exchange.getIn().setHeader(name,value);
  super.process(exchange);
}
