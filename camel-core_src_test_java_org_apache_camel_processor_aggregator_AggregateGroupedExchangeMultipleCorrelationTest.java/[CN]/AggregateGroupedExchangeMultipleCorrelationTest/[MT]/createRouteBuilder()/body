{
  return new RouteBuilder(){
    public void configure() throws Exception {
      from("direct:start").aggregate().header("foo").batchTimeout(1000L).groupExchanges().to("mock:result");
    }
  }
;
}
