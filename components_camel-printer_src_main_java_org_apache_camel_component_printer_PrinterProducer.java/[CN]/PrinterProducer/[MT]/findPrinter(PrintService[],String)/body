{
  int position=-1;
  for (int i=0; i < services.length; i++) {
    if (services[i].getName().toLowerCase().endsWith(printer.toLowerCase())) {
      position=i;
      break;
    }
  }
  return position;
}
