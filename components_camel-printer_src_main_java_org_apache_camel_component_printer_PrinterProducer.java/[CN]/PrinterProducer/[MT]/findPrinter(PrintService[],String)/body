{
  boolean found=false;
  int position=-1;
  for (int i=0; (i < services.length) && (!found); i++) {
    if (printer.equalsIgnoreCase(services[i].getName())) {
      found=true;
      position=i;
    }
  }
  return position;
}
