{
  super(endpoint);
  final PayloadFormat payloadFormat=endpoint.getConfiguration().getPayloadFormat();
  this.restClient=new DefaultRestClient(httpClient,endpointConfigMap.get(API_VERSION),payloadFormat.toString().toLowerCase(),session);
  this.classMap=endpoint.getComponent().getClassMap();
}
