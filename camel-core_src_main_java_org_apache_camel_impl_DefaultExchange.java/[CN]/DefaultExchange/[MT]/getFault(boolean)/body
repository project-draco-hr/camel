{
  if (fault == null && lazyCreate) {
    fault=(in != null && in instanceof MessageSupport) ? ((MessageSupport)in).newInstance() : createFaultMessage();
    configureMessage(fault);
  }
  return fault;
}
