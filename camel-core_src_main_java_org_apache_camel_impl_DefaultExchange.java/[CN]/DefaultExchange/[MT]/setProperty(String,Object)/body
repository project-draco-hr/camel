{
  if (name != null && name.startsWith("Camel")) {
    name=name.intern();
  }
  if (value != null) {
    getProperties().put(name,value);
  }
 else {
    if (name != null) {
      getProperties().remove(name);
    }
  }
}
