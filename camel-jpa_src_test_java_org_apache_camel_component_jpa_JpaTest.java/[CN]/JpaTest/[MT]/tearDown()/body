{
  if (transaction != null) {
    transaction.rollback();
    transaction=null;
  }
  if (entityManager != null) {
    entityManager.close();
  }
  stopServices(consumer,client,camelContext);
  super.tearDown();
}
