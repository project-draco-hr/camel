{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      rest("/hello").get().consumes("application/json").to("mock:foo").transform().constant("{ \"reply\": \"Bye World\" }");
    }
  }
;
}
