{
  for (  RouteDefinition route : getRoutes()) {
    RouteDefinitionHelper.prepareRoute(route,getOnExceptions(),getIntercepts(),getInterceptFroms(),getInterceptSendToEndpoints(),getOnCompletions());
    route.markPrepared();
  }
}
