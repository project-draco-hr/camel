{
  if (closeable != null) {
    try {
      closeable.close();
    }
 catch (    IOException e) {
      if (log != null) {
        log.warn("Cannot close: " + name + ". Reason: "+ e,e);
      }
    }
  }
}
