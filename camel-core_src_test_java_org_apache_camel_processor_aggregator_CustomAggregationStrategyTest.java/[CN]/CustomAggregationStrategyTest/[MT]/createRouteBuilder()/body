{
  return new RouteBuilder(){
    public void configure() throws Exception {
      from("direct:start").aggregator(new MyAggregationStrategy()).header("id").batchTimeout(2000L).to("mock:result");
    }
  }
;
}
