{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("servlet:///hello?transferException=true").throwException(new IllegalArgumentException("Damn"));
    }
  }
;
}
