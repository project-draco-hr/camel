{
  if (o instanceof PersonType) {
    PersonType that=(PersonType)o;
    return ObjectHelper.equal(this.firstName,that.firstName) && ObjectHelper.equal(this.lastName,that.lastName);
  }
  return false;
}
