{
  final Log log=LogFactory.getLog(category);
  return intercept(new DelegateProcessor<E>(){
    @Override public void process(    E exchange){
      log.trace(exchange);
      processNext(exchange);
    }
  }
);
}
