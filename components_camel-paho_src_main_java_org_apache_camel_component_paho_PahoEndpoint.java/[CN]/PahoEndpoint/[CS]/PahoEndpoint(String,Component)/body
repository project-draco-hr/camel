{
  super(uri,component);
  if (topic == null) {
    int optionIndex=uri.indexOf("?");
    if (optionIndex > 0) {
      topic=uri.substring(7,optionIndex);
    }
 else {
      topic=uri.substring(7);
    }
  }
}
