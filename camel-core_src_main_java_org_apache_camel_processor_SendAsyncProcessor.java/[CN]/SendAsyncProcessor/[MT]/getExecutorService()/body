{
  if (executorService == null) {
    executorService=destination.getCamelContext().getExecutorServiceStrategy().newThreadPool(this,"SendAsyncProcessor-Consumer",poolSize,poolSize);
  }
  return executorService;
}
