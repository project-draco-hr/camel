{
  if (producerExecutorService == null) {
    producerExecutorService=destination.getCamelContext().getExecutorServiceStrategy().newCachedThreadPool(this,"SendAsyncProcessor-Producer");
  }
  return producerExecutorService;
}
