{
  if (producerExecutorService == null) {
    producerExecutorService=destination.getCamelContext().getExecutorServiceStrategy().newCachedThreadPool("SendAsyncProcessor-Producer");
  }
  return producerExecutorService;
}
