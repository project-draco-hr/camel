{
  try {
    dataWriter.write(exchange.getIn().getMandatoryBody(SplunkEvent.class));
  }
 catch (  Exception e) {
    if (endpoint.reconnectIfPossible(e)) {
      dataWriter.start();
    }
    throw e;
  }
}
