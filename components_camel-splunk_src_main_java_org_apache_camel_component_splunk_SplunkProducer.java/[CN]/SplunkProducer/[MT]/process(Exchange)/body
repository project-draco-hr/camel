{
  try {
    if (!dataWriter.isConnected()) {
      dataWriter.start();
    }
    dataWriter.write(exchange.getIn().getMandatoryBody(SplunkEvent.class));
  }
 catch (  Exception e) {
    if (endpoint.reset(e)) {
      dataWriter.stop();
    }
    throw e;
  }
}
