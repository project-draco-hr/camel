{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      restConfiguration().component("restlet").host("localhost").port(portNum);
      rest("/users/").post("new").as(UserJaxbPojo.class).to("mock:input");
    }
  }
;
}
