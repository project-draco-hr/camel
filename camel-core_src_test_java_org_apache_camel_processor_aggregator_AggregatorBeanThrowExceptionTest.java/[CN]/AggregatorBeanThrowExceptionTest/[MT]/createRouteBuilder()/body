{
  return new RouteBuilder(){
    public void configure() throws Exception {
      from("direct:start").aggregate(header("id")).bean(AggregatorBeanThrowExceptionTest.class,"fooDoesNotExistMethod").to("log:foo");
    }
  }
;
}
