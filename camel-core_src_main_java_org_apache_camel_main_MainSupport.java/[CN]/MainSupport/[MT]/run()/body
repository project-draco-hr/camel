{
  if (!completed.get()) {
    start();
    try {
      afterStart();
      waitUntilCompleted();
      internalBeforeStop();
      beforeStop();
      stop();
    }
 catch (    Exception e) {
      LOG.error("Failed: " + e,e);
    }
  }
}
