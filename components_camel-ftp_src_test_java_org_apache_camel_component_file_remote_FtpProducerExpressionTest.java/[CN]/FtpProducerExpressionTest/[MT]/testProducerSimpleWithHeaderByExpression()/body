{
  template.sendBodyAndHeader(getFtpUrl() + "&expression=myfile-${in.header.foo}.txt","Hello World","foo","abc");
  Thread.sleep(500);
  assertFileExists(FTP_ROOT_DIR + "filelanguage/myfile-abc.txt");
}
