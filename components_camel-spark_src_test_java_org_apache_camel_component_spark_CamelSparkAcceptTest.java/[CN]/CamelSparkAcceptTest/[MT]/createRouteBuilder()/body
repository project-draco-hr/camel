{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("spark:get:hello?accept=application/json").to("mock:foo").transform().constant("{ \"reply\": \"Bye World\" }");
    }
  }
;
}
