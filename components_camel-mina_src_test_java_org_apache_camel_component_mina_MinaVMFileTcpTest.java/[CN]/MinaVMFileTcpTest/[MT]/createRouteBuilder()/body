{
  return new RouteBuilder(){
    public void configure(){
      from("mina:vm://localhost:9123").to("mock:results");
      from("file:src/test/data?noop=true").to("mina:vm://localhost:9123?lazySessionCreation=true");
    }
  }
;
}
