{
  if (o instanceof PurchaseOrder) {
    PurchaseOrder that=(PurchaseOrder)o;
    return ObjectHelper.equals(this.name,that.name) && ObjectHelper.equals(this.amount,that.amount) && ObjectHelper.equals(this.price,that.price);
  }
  return false;
}
