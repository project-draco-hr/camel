{
  ConfigurationHelper.populateFromURI(this,new ConfigurationHelper.ParameterSetter(){
    @Override public <T>void set(    EndpointConfiguration config,    String name,    T value){
      if (name != null && value != null) {
        params.put(name,value);
      }
    }
  }
);
}
