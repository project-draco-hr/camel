{
  OutboundBinding binding=resolveAndRemoveReferenceParameter(parameters,"outboundBindingRef",OutboundBinding.class,new GMailBinding());
  MailService service=resolveAndRemoveReferenceParameter(parameters,"mailServiceRef",MailService.class,MailServiceFactory.getMailService());
  GMailEndpoint endpoint=new GMailEndpoint(uri,remaining);
  endpoint.setOutboundBinding(binding);
  endpoint.setMailService(service);
  return endpoint;
}
