{
  System.out.println("Shutting down FTP server");
  LOG.info("Shutting down FTP server with port " + port);
  try {
    ftpServer.stop();
    ftpServer=null;
    port=0;
  }
 catch (  Exception e) {
  }
}
