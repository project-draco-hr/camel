{
  if (ref != null) {
    return getShortName() + "[ref:" + ref+ "]";
  }
 else   if (policy != null) {
    return getShortName() + "[" + policy.toString()+ "]";
  }
 else {
    return getShortName();
  }
}
