{
  if (!completed.get()) {
    try {
      start();
      waitUntilCompleted();
      stop();
    }
 catch (    Exception e) {
      System.out.println("Failed: " + e);
      e.printStackTrace();
    }
  }
}
