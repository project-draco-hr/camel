{
  if (!completed.get()) {
    try {
      start();
      postProcessContext();
      waitUntilCompleted();
      stop();
    }
 catch (    Exception e) {
      LOG.error("Failed: " + e,e);
    }
  }
}
