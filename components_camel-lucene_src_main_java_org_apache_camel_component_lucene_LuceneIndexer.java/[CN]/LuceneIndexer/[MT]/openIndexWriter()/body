{
  IndexWriterConfig indexWriterConfig;
  indexWriterConfig=new IndexWriterConfig(Version.LUCENE_45,getAnalyzer()).setOpenMode(OpenMode.CREATE_OR_APPEND);
  indexWriter=new IndexWriter(niofsDirectory,indexWriterConfig);
}
