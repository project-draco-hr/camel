{
  return new RouteBuilder(){
    public void configure(){
      from("mina:udp://localhost:9123").to("mock:results");
      from("file:src/test/data?noop=true").to("mina:udp://localhost:9123");
    }
  }
;
}
