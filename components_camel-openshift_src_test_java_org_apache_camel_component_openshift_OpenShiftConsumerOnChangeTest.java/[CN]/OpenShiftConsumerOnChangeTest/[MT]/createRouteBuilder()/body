{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      fromF("openshift:myApp?username=%s&password=%s&delay=5s&pollMode=onChange",username,password).log("Event ${header.CamelOpenShiftEventType} for app ${body.name}").to("mock:result");
    }
  }
;
}
