{
  return new RouteBuilder(){
    public void configure() throws Exception {
      from("direct:in").to("bean:myBean").setHeader(FileComponent.HEADER_FILE_NAME,constant("BeanToFileTest.txt")).to("file://target/?append=false");
    }
  }
;
}
