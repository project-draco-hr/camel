{
  for (  String name : names) {
    try {
      T bean=context.getRegistry().lookupByNameAndType(name,type);
      if (bean != null) {
        return bean;
      }
    }
 catch (    Exception e) {
    }
  }
  return null;
}
