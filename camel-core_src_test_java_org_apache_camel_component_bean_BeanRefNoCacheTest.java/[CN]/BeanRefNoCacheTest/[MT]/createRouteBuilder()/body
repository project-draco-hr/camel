{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("direct:start").beanRef(MyCoolBean.class.getName(),"doSomething",false).to("mock:result");
    }
  }
;
}
