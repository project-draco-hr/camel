{
  String sqlTypeAsStr;
  String name;
  Token valueSrcToken;
  sqlTypeAsStr=ParameterSqlType();
  jj_consume_token(4);
  valueSrcToken=InputParameterSrc();
  int sqlType=ParseHelper.parseSqlType(sqlTypeAsStr);
{
    if (true)     return new InputParameter(createNextParameterName(),sqlType,valueSrcToken,ParseHelper.sqlTypeToJavaType(sqlType,sqlTypeAsStr));
  }
  throw new Error("Missing return statement in function");
}
