{
  return new RouteBuilder(){
    public void configure(){
      from("direct:start").marshal().csv().to("mock:result");
      from("file:src/test/resources/daltons.csv?noop=true").unmarshal().csv().to("mock:daltons");
    }
  }
;
}
