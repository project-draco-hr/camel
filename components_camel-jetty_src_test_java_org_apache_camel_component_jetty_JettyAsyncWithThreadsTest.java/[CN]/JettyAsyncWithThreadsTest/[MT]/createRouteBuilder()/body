{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("jetty:" + ENDPOINT_NAME).transform(body().append(" World")).threads().waitForTaskToComplete(WaitForTaskToComplete.Never).to("mock:foo").delay(100).process(new MyProcessor()).to("mock:result");
    }
  }
;
}
