{
  try {
    if (calendar != null) {
      requestBody("google-calendar://calendars/delete?inBody=calendarId",getCalendar().getId());
      setCalendar(null);
    }
  }
 catch (  Exception e) {
  }
}
