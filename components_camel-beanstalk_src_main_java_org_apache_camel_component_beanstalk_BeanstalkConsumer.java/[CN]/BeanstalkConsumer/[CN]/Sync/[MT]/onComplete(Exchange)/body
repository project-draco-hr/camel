{
  try {
    executor.submit(new RunCommand(successCommand,exchange)).get();
  }
 catch (  Exception e) {
    if (log.isErrorEnabled())     log.error(String.format("Could not run completion of exchange %s",exchange),e);
  }
}
