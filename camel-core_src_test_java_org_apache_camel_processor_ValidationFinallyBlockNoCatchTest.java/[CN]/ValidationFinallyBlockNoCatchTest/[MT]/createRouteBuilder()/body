{
  return new RouteBuilder(){
    public void configure(){
      errorHandler(deadLetterChannel().delay(25));
      TryType tryType=from("direct:start").tryBlock().process(validator).to("mock:valid");
      tryType.finallyBlock().to("mock:all");
    }
  }
;
}
