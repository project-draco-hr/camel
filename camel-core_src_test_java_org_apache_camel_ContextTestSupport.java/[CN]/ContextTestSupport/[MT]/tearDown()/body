{
  try {
    log.debug("tearDown test: " + getName());
    template.stop();
    stopCamelContext();
  }
 catch (  Exception e) {
    log.debug("tearDown ignored exception while stopping: " + e.getMessage());
  }
}
