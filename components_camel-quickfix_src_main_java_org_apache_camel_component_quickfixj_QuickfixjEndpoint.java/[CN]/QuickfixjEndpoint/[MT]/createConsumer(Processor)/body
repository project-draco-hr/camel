{
  LOG.info("Creating QuickFIX/J consumer: " + (sessionID != null ? sessionID : "No Session") + ", ExchangePattern="+ getExchangePattern());
  QuickfixjConsumer consumer=new QuickfixjConsumer(this,processor);
  consumers.add(consumer);
  return consumer;
}
