{
  return new RouteBuilder(){
    public void configure() throws Exception {
      from("newfile://target/includeexclude/?includeNamePrefix=report&excludeNamePostfix=xml").to("mock:result");
    }
  }
;
}
