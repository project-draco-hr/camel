{
  return new RouteBuilder(){
    public void configure() throws Exception {
      from("file://target/includeexclude/?includeNamePrefix=report&excludeNamePostfix=xml").to("mock:result");
    }
  }
;
}
