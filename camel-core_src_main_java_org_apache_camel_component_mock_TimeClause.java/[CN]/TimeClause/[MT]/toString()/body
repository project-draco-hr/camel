{
  if (timeFrom == null) {
    return "no later than " + timeTo + " "+ getOperationText()+ " ("+ was+ ")";
  }
 else {
    return "between " + timeFrom.getNumber() + "-"+ timeTo.getNumber()+ " "+ timeTo.getTimeUnit().toString().toLowerCase()+ " "+ getOperationText()+ " ("+ was+ ")";
  }
}
