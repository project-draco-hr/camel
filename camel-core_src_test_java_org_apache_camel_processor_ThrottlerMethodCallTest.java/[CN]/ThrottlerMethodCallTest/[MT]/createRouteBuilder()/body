{
  return new RouteBuilder(){
    public void configure(){
      from("direct:expressionMethod").throttle(method("myBean","getMessagesPerSecond")).timePeriodMillis(INTERVAL).to("log:result","mock:result");
    }
  }
;
}
