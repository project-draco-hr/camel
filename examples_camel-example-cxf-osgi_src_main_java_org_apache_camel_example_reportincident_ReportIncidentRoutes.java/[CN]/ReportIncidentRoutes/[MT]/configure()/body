{
  OutputReportIncident ok=new OutputReportIncident();
  ok.setCode("0");
  from("cxf:bean:reportIncident").convertBodyTo(InputReportIncident.class).setHeader(Exchange.FILE_NAME,constant("request-${date:now:yyyy-MM-dd-HHmmssSSS}")).to("file://target/inbox/").transform(constant(ok));
}
