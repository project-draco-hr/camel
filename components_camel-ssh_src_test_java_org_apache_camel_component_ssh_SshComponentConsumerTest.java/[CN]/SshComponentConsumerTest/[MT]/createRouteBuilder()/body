{
  return new RouteBuilder(){
    @Override public void configure(){
      from("ssh://smx:smx@localhost:" + port + "?useFixedDelay=true&delay=5000&pollCommand=test%0D").to("mock:result");
    }
  }
;
}
