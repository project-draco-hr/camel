{
  Properties props=new Properties();
  props.put("component",EventAdminComponent.NAME);
  registration=bundleContext.registerService(ComponentResolver.class.getName(),new EventAdminServiceFactory(),props);
}
