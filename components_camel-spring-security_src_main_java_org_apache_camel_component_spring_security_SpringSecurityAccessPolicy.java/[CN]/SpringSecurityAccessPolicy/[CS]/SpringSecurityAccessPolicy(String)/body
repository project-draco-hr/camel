{
  Assert.isTrue(access != null,"The access attribute must not be null.");
  String[] accessValues=StringUtils.trimArrayElements(StringUtils.commaDelimitedListToStringArray(access));
  this.configAttributeDefinition=(accessValues.length > 0) ? new ConfigAttributeDefinition(accessValues) : null;
}
