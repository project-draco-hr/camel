{
  from("timer:trigger?exchangePattern=InOut&period=10s").bean("counterBean").hystrix().log(" Client request: ${body}").to("http://localhost:9090/service1").onFallback().log(" Client fallback request: ${body}").to("http://localhost:9090/service2").end().log("Client response: ${body}");
}
