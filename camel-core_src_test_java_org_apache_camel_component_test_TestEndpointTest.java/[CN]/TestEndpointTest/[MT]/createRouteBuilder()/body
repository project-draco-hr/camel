{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      template.sendBody("seda:foo","Hello World");
      template.sendBody("seda:foo","Bye World");
      from("seda:foo").to("test:seda:foo");
    }
  }
;
}
