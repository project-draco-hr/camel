{
  return new RouteBuilder(){
    public void configure() throws Exception {
      errorHandler(noErrorHandler());
      from("seda:in").thread(5).transform().xquery("/person/id",String.class).to("mock:result");
    }
  }
;
}
