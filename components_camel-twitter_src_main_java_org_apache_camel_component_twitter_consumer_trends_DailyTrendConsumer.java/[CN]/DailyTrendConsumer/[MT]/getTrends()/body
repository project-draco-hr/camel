{
  Date date=te.getProperties().parseDate();
  if (date != null) {
    return te.getProperties().getTwitter().getDailyTrends(date,false);
  }
 else {
    return te.getProperties().getTwitter().getDailyTrends();
  }
}
