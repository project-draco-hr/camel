{
  StreamCache newBody=in.getBody(StreamCache.class);
  if (newBody != null) {
    in.setBody(newBody);
  }
  Object answer=in.getBody(String.class);
  if (answer == null) {
    answer=in.getBody();
  }
  if (newBody != null) {
    newBody.reset();
  }
  return answer;
}
