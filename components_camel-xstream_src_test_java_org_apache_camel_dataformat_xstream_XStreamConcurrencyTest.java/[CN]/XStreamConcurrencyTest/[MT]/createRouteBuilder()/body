{
  return new RouteBuilder(){
    public void configure(){
      from("direct:start").marshal().xstream().to("direct:marshalled");
      from("direct:marshalled").unmarshal().xstream().to("mock:result");
    }
  }
;
}
