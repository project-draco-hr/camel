{
  DeadLetterChannel answer=new DeadLetterChannel(processor,getFailureProcessor(),deadLetterUri,onRedelivery,getRedeliveryPolicy(),getLogger(),getExceptionPolicyStrategy(),getHandledPolicy(),isUseOriginalBody());
  configure(answer);
  return answer;
}
