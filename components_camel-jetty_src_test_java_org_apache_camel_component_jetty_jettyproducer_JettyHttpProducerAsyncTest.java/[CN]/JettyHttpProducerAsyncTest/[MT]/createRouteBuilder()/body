{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("direct:start").toAsync("jetty://http://" + localServer.getServiceHostName() + ":"+ localServer.getServicePort()+ "?throwExceptionOnFailure=false").to("mock:result");
    }
  }
;
}
