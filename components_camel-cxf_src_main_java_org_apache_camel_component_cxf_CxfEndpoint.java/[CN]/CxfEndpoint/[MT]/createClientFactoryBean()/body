{
  return new ClientFactoryBean(new WSDLServiceFactoryBean()){
    @Override protected void createClient(    Endpoint ep){
      setClient(new CamelCxfClientImpl(getBus(),ep));
    }
    @Override protected void initializeAnnotationInterceptors(    Endpoint ep,    Class<?> cls){
    }
  }
;
}
