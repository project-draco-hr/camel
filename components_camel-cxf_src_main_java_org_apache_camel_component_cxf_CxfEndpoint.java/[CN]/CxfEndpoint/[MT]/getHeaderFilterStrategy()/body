{
  if (headerFilterStrategy == null) {
    headerFilterStrategy=new CxfHeaderFilterStrategy();
    if (LOG.isDebugEnabled()) {
      LOG.debug("Create CXF default header filter strategy " + headerFilterStrategy);
    }
  }
  return headerFilterStrategy;
}
