{
  return new RouteBuilder(){
    public void configure(){
      from("direct:start").setHeader(Exchange.HTTP_PATH,constant("search")).setHeader(Exchange.HTTP_QUERY,constant("hl=en&q=activemq")).to("http://www.google.com").to("mock:results");
    }
  }
;
}
