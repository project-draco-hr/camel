{
  return new RouteBuilder(){
    public void configure(){
      from("direct:start").setHeader(HttpConstants.HTTP_PATH,constant("search")).setHeader(HttpConstants.HTTP_QUERY,constant("hl=en&q=activemq")).to("http://www.google.com").to("mock:results");
    }
  }
;
}
