{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("direct:start").unmarshal().bindy(BindyType.Csv,"org.apache.camel.dataformat.bindy.csv2").to("mock:result");
    }
  }
;
}
