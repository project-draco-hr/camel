{
  String DSL="from(\"direct:start\").choice()" + ".when().method(\"controlBean\", \"isDetour\").to(\"mock:detour\")" + ".end()"+ ".to(\"mock:result\")";
  String expectedDSL=DSL;
  assertEquals(expectedDSL,render(DSL));
}
