{
  String dsl="from(\"direct:start\").choice()" + ".when(header(\"username\").isNull()).to(\"mock:god\")" + ".when(header(\"admin\").isEqualTo(\"true\")).to(\"mock:admin\")"+ ".otherwise().to(\"mock:guest\")"+ ".end()";
  assertEquals(dsl,render(dsl));
}
