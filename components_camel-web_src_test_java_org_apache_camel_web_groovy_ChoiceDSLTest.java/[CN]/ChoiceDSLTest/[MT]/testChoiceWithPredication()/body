{
  String DSL="from(\"direct:start\").choice()" + ".when(header(\"username\").isNotNull()).to(\"mock:god\")" + ".when(header(\"admin\").isEqualTo(\"true\")).to(\"mock:admin\")"+ ".otherwise().to(\"mock:guest\")"+ ".end()";
  String expectedDSL=DSL;
  assertEquals(expectedDSL,render(DSL));
}
