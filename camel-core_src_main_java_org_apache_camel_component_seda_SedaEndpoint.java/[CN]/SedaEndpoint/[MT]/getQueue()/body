{
  if (queue == null) {
    if (getComponent() != null) {
      queue=getComponent().getOrCreateQueue(getEndpointUri(),getSize());
    }
 else {
      queue=createQueue();
    }
  }
  return queue;
}
