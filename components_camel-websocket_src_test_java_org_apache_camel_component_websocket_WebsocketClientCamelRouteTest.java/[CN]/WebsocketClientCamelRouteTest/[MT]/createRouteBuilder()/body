{
  return new RouteBuilder(){
    public void configure(){
      from("websocket://test").log(">>> Message received from WebSocket Client : ${body}").loop(10).setBody().constant(">> Welcome on board!").to("websocket://test");
    }
  }
;
}
