{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("file://target/enrich?move=.done&readLock=none").to("mock:start").pollEnrich("file://target/enrichdata?move=.done&readLock=none").to("mock:result");
    }
  }
;
}
