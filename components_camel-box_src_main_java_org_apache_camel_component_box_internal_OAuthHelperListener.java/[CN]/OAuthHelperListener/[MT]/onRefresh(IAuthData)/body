{
  if (authSecureStorage != null && (newAuthData == null || !newAuthData.getRefreshToken().equals(refreshToken))) {
    authSecureStorage.saveAuth(newAuthData);
  }
  if (configListener != null) {
    configListener.onRefresh(newAuthData);
  }
}
