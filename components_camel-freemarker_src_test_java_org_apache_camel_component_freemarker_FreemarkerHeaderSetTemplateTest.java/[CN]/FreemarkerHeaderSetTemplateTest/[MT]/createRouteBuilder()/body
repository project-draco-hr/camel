{
  return new RouteBuilder(){
    public void configure(){
      from("direct:a").setHeader(FreemarkerConstants.FREEMARKER_RESOURCE_URI).constant("org/apache/camel/component/freemarker/example.ftl").to("freemarker:dummy");
    }
  }
;
}
