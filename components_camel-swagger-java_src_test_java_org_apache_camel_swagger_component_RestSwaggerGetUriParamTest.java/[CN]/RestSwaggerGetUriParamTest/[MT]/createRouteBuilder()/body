{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      restConfiguration().producerComponent("dummy");
      from("direct:start").to("rest:get:bye?name={name}&apiDoc=hello-api.json").to("mock:result");
    }
  }
;
}
