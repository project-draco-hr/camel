{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("direct:injectByName").beanRef("shoppingBean","listAllProducts").to("mock:result");
      from("direct:injectByType").bean(ShoppingBean.class,"listAllProducts").to("mock:result");
    }
  }
;
}
