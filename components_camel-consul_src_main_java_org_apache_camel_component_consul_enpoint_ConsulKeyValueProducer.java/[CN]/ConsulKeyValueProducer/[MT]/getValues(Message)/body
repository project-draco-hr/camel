{
  Object result;
  if (isValueAsString(message)) {
    result=getClient().getValuesAsString(getMandatoryKey(message));
  }
 else {
    result=getClient().getValues(getMandatoryKey(message),getOption(message,QueryOptions.BLANK,QueryOptions.class));
  }
  setBodyAndResult(message,result);
}
