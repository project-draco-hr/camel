{
  ((RouteboxSedaEndpoint)getRouteboxEndpoint()).onStarted(this);
  doStartInnerContext();
  int poolSize=getRouteboxEndpoint().getConfig().getThreads();
  setExecutor(((RouteboxSedaEndpoint)getRouteboxEndpoint()).getCamelContext().getExecutorServiceStrategy().newFixedThreadPool(this,((RouteboxSedaEndpoint)getRouteboxEndpoint()).getEndpointUri(),poolSize));
  for (int i=0; i < poolSize; i++) {
    getExecutor().execute((Runnable)this);
  }
}
