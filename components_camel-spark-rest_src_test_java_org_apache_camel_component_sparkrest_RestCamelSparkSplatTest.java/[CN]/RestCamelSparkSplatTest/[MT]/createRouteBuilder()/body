{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      rest().path("/hello/*/to/*").get().to("mock:foo").transform().simple("Bye big ${header.splat[1]} from ${header.splat[0]}");
    }
  }
;
}
