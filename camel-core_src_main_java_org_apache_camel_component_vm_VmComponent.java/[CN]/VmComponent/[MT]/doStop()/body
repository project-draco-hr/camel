{
  super.doStop();
  if (START_COUNTER.decrementAndGet() == 0) {
synchronized (QUEUES) {
      for (      BlockingQueue<Exchange> q : QUEUES.values()) {
        q.clear();
      }
      QUEUES.clear();
    }
  }
}
