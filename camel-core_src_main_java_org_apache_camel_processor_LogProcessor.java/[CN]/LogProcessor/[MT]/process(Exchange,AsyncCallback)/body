{
  try {
    String msg=expression.evaluate(exchange,String.class);
    logger.log(msg);
  }
 catch (  Exception e) {
    exchange.setException(e);
  }
 finally {
    callback.done(true);
  }
  return true;
}
