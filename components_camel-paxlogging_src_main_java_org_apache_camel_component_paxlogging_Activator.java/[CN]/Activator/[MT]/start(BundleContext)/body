{
  Properties props=new Properties();
  props.put("component",PaxLoggingComponent.NAME);
  registration=bundleContext.registerService(ComponentResolver.class.getName(),new PaxLoggingServiceFactory(),props);
}
