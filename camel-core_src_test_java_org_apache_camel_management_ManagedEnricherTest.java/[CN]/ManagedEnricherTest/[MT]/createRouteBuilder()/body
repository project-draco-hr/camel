{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("direct:start").enrich().simple("direct:${header.whereto}").id("mysend");
      from("direct:foo").to("mock:foo");
    }
  }
;
}
