{
  Map<String,Object> models=IntrospectionSupport.extractProperties(parameters,PREFIX_CACHE);
  for (  Map.Entry<String,Object> entry : models.entrySet()) {
    addCacheConfiguration(entry.getKey(),EndpointHelper.resolveParameter(context,(String)entry.getValue(),CacheConfiguration.class));
  }
  return this;
}
