{
  if (session.getRepositoryInfo().getCmisVersion() == CmisVersion.CMIS_1_0)   return false;
  for (  ObjectType type : session.getTypeChildren(null,false)) {
    if (BaseTypeId.CMIS_SECONDARY.value().equals(type.getId())) {
      return true;
    }
  }
  return false;
}
