{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("seda:start").delay(50).to("sjms:in.out.temp.queue?exchangePattern=InOut&synchronous=false").to("mock:result");
      from("sjms:in.out.temp.queue?exchangePattern=InOut&synchronous=false").log("Using ${threadName} to process ${body}").delay(50).transform(body().prepend("Bye "));
    }
  }
;
}
