{
  if (type.isEnum()) {
    return "enum";
  }
 else   if (type.isArray()) {
    return "array";
  }
  String primitive=getPrimitiveType(type);
  if (primitive != null) {
    return primitive;
  }
  return "object";
}
