{
  if (!completed.get()) {
    start();
    try {
      afterStart();
      waitUntilCompleted();
      beforeStop();
      stop();
    }
 catch (    Exception e) {
      LOG.error("Failed: " + e,e);
    }
  }
}
