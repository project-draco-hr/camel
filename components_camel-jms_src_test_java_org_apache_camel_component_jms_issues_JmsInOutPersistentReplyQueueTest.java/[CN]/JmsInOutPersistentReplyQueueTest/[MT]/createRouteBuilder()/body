{
  return new RouteBuilder(){
    public void configure() throws Exception {
      from("seda:start").log("Sending ${body}").inOut("activemq:queue:foo?replyTo=myReplies").threads(5).log("Reply ${body}").delay(2000).to("mock:result");
      from("activemq:queue:foo").to("mock:foo").transform(body().prepend("Bye ")).log("Sending back reply ${body}");
    }
  }
;
}
