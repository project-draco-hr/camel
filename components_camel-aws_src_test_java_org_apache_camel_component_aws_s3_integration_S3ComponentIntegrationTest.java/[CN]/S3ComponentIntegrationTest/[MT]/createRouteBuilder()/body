{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("direct:start").to("aws-s3://mycamelbucket?accessKey=xxx&secretKey=yyy&region=us-west-1");
      from("aws-s3://mycamelbucket?accessKey=xxx&secretKey=yyy&region=us-west-1").to("mock:result");
    }
  }
;
}
