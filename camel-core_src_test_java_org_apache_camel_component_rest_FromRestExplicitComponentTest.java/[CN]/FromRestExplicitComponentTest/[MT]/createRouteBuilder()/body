{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      rest("/say/hello").component("dummy-rest").get().to("direct:hello");
      rest().component("dummy-rest").uri("/say/bye").get().consumes("application/json").to("direct:bye").endRest().post().to("mock:update");
      from("direct:hello").transform().constant("Hello World");
      from("direct:bye").transform().constant("Bye World");
    }
  }
;
}
