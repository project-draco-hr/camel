{
  RouteBuilder builder=new RouteBuilder(){
    public void configure(){
      from("queue:a").idempotentConsumer(header("myMessageId"),memoryMessageIdRepository(200)).to("queue:b");
    }
  }
;
  return builder.getRouteList();
}
