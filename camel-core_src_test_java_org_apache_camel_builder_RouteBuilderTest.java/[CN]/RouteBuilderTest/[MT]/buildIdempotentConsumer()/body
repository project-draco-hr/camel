{
  RouteBuilder builder=new RouteBuilder(){
    public void configure(){
      from("seda:a").idempotentConsumer(header("myMessageId"),memoryMessageIdRepository(200)).to("seda:b");
    }
  }
;
  return getRouteList(builder);
}
