{
  return new RouteBuilder(){
    Namespaces ns=new Namespaces("","http:acme.com");
    @Override public void configure() throws Exception {
      from("file:target/pair").split().xtokenize("//order",'w',ns,2).to("log:split").to("mock:split");
    }
  }
;
}
