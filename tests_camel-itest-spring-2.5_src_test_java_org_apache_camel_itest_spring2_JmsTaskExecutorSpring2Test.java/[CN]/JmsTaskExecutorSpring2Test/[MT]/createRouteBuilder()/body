{
  return new RouteBuilder(){
    public void configure() throws Exception {
      from("activemq:foo").to("log:input").to("mock:result").transform(constant("Bye World")).to("log:output");
    }
  }
;
}
