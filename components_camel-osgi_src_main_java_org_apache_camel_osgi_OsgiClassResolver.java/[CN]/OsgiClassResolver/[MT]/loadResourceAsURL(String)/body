{
  ObjectHelper.notEmpty(uri,"uri");
  URL url=null;
  for (  Bundle bundle : bundleContext.getBundles()) {
    url=bundle.getEntry(uri);
    if (url != null) {
      break;
    }
  }
  return url;
}
