{
  from("ghttp:///test1").to(getBaseUri("ghttp") + "/test");
  from("ghttp:///test2").setHeader(Exchange.HTTP_URI).constant(getBaseUri() + "/blah").to(getBaseUri("ghttp") + "/test?bridgeEndpoint=false");
}
