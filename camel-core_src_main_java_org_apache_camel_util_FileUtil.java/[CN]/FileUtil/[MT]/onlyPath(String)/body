{
  if (name == null) {
    return null;
  }
  int pos=name.lastIndexOf('/');
  if (pos > 0) {
    return name.substring(0,pos);
  }
 else   if (pos == 0) {
    return name;
  }
 else {
    pos=name.lastIndexOf(File.separator);
  }
  return null;
}
