{
  if (path != null && isWindows() && path.indexOf('/') >= 0) {
    return path.replace('/','\\');
  }
 else   if (path != null && path.indexOf('\\') >= 0) {
    return path.replace('\\','/');
  }
  return path;
}
