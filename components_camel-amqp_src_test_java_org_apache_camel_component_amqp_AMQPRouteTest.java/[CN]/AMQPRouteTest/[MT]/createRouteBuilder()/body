{
  return new RouteBuilder(){
    public void configure() throws Exception {
      from("amqp0-9:queue:ping").to("log:routing").to("mock:result");
      from("amqp1-0:queue:ping").to("log:routing").to("mock:result");
    }
  }
;
}
