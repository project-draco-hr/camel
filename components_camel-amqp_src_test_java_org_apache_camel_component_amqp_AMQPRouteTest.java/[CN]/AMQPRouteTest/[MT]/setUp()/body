{
  try {
    TransportConnection.createVMBroker(1);
  }
 catch (  Exception e) {
    TransportConnection.createVMBroker(1);
  }
  super.setUp();
  resultEndpoint=(MockEndpoint)context.getEndpoint("mock:result");
}
