{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("jetty://http://0.0.0.0:9080/myservice").delay(2000).transform().constant("Bye World").to("mock:result");
    }
  }
;
}
