{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("activemq:topic:foo").routeId("a").to("mock:a");
      from("activemq:topic:foo").routeId("b").to("mock:b");
    }
  }
;
}
