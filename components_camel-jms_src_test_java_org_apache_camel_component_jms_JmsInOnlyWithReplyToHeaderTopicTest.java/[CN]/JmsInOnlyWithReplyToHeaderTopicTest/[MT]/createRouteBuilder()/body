{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("activemq:queue:foo").transform(body().prepend("Hello ")).to("mock:result");
      from("activemq:topic:bar").to("mock:bar");
    }
  }
;
}
