{
  return new RouteBuilder(){
    public void configure() throws Exception {
      errorHandler(noErrorHandler());
      from("direct:start").thread(5).to("xquery:org/apache/camel/component/xquery/transform.xquery").to("mock:result");
    }
  }
;
}
