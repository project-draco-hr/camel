{
  return new RouteBuilder(){
    public void configure(){
      from("jetty:http://localhost:9080/myworld").streamCaching().choice().when().xpath("/one").to("mock:x").when().xpath("/two").to("mock:y").otherwise().to("mock:z").end();
    }
  }
;
}
