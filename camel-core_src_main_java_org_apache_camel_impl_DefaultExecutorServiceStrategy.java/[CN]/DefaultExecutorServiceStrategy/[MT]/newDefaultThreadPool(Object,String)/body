{
  ExecutorService answer=ExecutorServiceHelper.newThreadPool(threadNamePattern,name,defaultThreadPoolProfile.getPoolSize(),defaultThreadPoolProfile.getMaxPoolSize(),defaultThreadPoolProfile.getKeepAliveTime(),defaultThreadPoolProfile.getTimeUnit(),defaultThreadPoolProfile.getMaxQueueSize(),false);
  onNewExecutorService(answer);
  return answer;
}
