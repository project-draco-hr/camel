{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("direct:start").to("xslt:org/apache/camel/itest/osgi/core/xslt/example.xsl?saxon=true").to("log:result").to("mock:result");
    }
  }
;
}
