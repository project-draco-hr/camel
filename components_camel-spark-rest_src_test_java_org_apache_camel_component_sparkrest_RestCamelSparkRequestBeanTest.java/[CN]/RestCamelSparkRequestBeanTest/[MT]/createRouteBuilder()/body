{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      rest("/hello/*/to/*").get().to("mock:foo").bean(RestCamelSparkRequestBeanTest.class,"doSomething");
    }
  }
;
}
