{
  if (compare(lhs,rhs)) {
    return true;
  }
  if (compare(lhs.getMessage(),rhs.getMessage())) {
    return true;
  }
  if (compare(lhs.getOriginator(),rhs.getOriginator())) {
    return true;
  }
  return false;
}
