{
  return new RouteBuilder(){
    public void configure() throws Exception {
      from("file://target/exclude/?exclude=^secret.*|.*xml$&caseSensitive=false").convertBodyTo(String.class).to("mock:result");
    }
  }
;
}
