{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("direct:start").aggregate(header("id"),new MyAggregationStrategy()).completionInterval(5000).aggregationRepository(repo).to("mock:aggregated");
    }
  }
;
}
