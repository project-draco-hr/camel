{
  String uuid=null;
  if (exchange != null) {
    uuid=exchange.getContext().getUuidGenerator().generateUuid();
  }
  if (uuid == null) {
    uuid=new JavaUuidGenerator().generateUuid();
  }
  return uuid;
}
