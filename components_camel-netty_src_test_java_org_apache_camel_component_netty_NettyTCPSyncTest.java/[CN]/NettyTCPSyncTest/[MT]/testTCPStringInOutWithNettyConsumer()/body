{
  if (LOG.isDebugEnabled()) {
    LOG.debug("Beginning Test ---> testTCPInOutWithNettyConsumer()");
  }
  String response=producerTemplate.requestBody("netty:tcp://localhost:5150?sync=true","Epitaph in Kohima, India marking the WWII Battle of Kohima and Imphal, Burma Campaign - Attributed to John Maxwell Edmonds",String.class);
  assertEquals("When You Go Home, Tell Them Of Us And Say, For Your Tomorrow, We Gave Our Today.",response);
  if (LOG.isDebugEnabled()) {
    LOG.debug("Completed Test ---> testTCPInOutWithNettyConsumer()");
  }
}
