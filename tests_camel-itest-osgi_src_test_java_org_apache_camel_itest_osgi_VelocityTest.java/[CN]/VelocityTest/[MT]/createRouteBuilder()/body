{
  return new RouteBuilder(){
    public void configure(){
      Thread.currentThread().setContextClassLoader(this.getClass().getClassLoader());
      from("direct:a").to("velocity:org/apache/camel/itest/osgi/example.vm");
    }
  }
;
}
