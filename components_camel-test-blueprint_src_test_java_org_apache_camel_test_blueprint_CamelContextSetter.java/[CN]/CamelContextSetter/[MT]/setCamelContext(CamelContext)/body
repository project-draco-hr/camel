{
  MyInflightRepository repository=new MyInflightRepository();
  repository.setWaitTime(100);
  repository.setTimeout(100);
  camelContext.addService(repository);
  camelContext.setInflightRepository(repository);
}
