{
  if (scheduler != null) {
    if (LOG.isDebugEnabled()) {
      LOG.debug("Shutting down Quartz scheduler: " + scheduler.getSchedulerName());
    }
    scheduler.shutdown();
  }
  super.doStop();
}
