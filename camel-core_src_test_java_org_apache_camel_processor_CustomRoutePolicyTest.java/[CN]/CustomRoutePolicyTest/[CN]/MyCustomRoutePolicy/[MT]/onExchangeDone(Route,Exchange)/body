{
  String body=exchange.getIn().getBody(String.class);
  if ("stop".equals(body)) {
    try {
      stopConsumer(route.getConsumer());
    }
 catch (    Exception e) {
      handleException(e);
    }
  }
}
