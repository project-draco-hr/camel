{
  return new RouteBuilder(){
    public void configure(){
      from(uriConsumer).log(">>> Message received from WebSocket Client : ${body}").to("mock:client").loop(10).setBody().constant(">> Welcome on board!").to(uriConsumer);
    }
  }
;
}
