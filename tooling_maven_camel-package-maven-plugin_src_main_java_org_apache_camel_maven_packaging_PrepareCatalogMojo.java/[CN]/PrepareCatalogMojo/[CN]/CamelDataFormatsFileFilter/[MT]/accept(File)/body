{
  if (pathname.isDirectory() && pathname.getName().equals("model")) {
    return false;
  }
  return pathname.isDirectory() || pathname.getName().endsWith(".json") || (pathname.isFile() && pathname.getName().equals("dataformat.properties"));
}
