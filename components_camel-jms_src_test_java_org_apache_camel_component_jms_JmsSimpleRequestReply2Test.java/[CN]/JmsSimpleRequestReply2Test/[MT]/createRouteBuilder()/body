{
  return new RouteBuilder(){
    public void configure() throws Exception {
      from("direct:start").inOut("activemq:queue:foo").to("mock:result");
      from("activemq:queue:foo").transform(body().prepend("Hello "));
    }
  }
;
}
