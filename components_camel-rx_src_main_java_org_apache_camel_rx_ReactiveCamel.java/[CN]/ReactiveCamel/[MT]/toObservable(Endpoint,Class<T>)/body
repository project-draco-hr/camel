{
  return createEndpointObservable(endpoint,new Func1<Exchange,T>(){
    @Override public T call(    Exchange exchange){
      Message in=exchange.getIn();
      return in.getBody(bodyType);
    }
  }
);
}
