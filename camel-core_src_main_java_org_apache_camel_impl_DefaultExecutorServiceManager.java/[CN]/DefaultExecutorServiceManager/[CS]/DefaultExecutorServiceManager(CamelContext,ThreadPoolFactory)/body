{
  this.camelContext=camelContext;
  this.threadPoolFactory=threadPoolFactory;
  this.defaultProfile=new ThreadPoolBuilder("defaultThreadPoolProfile").poolSize(10).maxPoolSize(20).keepAliveTime(60L,TimeUnit.SECONDS).maxQueueSize(1000).rejectedPolicy(ThreadPoolRejectedPolicy.CallerRuns).build();
}
