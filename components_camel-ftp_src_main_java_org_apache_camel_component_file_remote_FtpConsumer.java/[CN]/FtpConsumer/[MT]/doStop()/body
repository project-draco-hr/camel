{
  log.info("Stopping");
  try {
    disconnect();
  }
 catch (  Exception e) {
    String message="Could not disconnect from " + remoteServer() + ". Reason: "+ client.getReplyString()+ ". Code: "+ client.getReplyCode();
    log.warn(message);
  }
  super.doStop();
}
