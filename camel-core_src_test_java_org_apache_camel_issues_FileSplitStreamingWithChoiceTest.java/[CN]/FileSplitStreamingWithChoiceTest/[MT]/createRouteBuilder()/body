{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("file://target/filesplit").split(body().tokenize("\n")).streaming().to("mock:split").choice().when(body(String.class).isNotNull()).to("mock:body").otherwise().to("mock:other").end();
    }
  }
;
}
