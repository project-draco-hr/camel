{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("file://target/in?recursive=true").to("ftp://admin:admin@localhost:" + getPort() + "//out/?fileName=${file:name}&tempFileName=${file:onlyname}.part&stepwise=false");
    }
  }
;
}
