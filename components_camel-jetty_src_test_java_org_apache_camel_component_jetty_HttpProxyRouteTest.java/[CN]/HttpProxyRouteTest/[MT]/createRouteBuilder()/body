{
  return new RouteBuilder(){
    public void configure(){
      from("jetty://http://localhost:9080/hello").to("http://localhost:9080/bye?throwExceptionOnFailure=false&bridgeEndpoint=true");
      from("jetty://http://localhost:9080/bye").transform(header("foo").prepend("Bye "));
    }
  }
;
}
