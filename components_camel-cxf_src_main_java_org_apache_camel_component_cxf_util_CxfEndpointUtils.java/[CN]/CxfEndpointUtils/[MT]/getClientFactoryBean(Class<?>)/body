{
  try {
    boolean isJSR181SEnabled=CxfEndpointUtils.hasWebServiceAnnotation(cls);
    ClientFactoryBean clientFactory=isJSR181SEnabled ? new JaxWsClientFactoryBean() : new ClientFactoryBean();
    return clientFactory;
  }
 catch (  Exception e) {
    throw new CamelException(e);
  }
}
