{
  try {
    boolean isJSR181SEnabled=CxfEndpointUtils.hasWebServiceAnnotation(cls);
    ServerFactoryBean serverFactory=isJSR181SEnabled ? new JaxWsServerFactoryBean() : new ServerFactoryBean();
    return serverFactory;
  }
 catch (  Exception e) {
    throw new CamelException(e);
  }
}
