{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      context.addComponent("github",new GitHubComponent());
      from("github://pullRequestComment?" + GITHUB_CREDENTIALS_STRING).process(new PullRequestCommentProcessor()).to(mockResultEndpoint);
    }
  }
;
}
