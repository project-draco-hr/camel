{
  File[] files=dir.listFiles(filter);
  if (files != null) {
    for (    File file : files) {
      if (file.isFile()) {
        found.add(file);
      }
 else       if (file.isDirectory()) {
        findFilesRecursive(file,found,filter);
      }
    }
  }
}
