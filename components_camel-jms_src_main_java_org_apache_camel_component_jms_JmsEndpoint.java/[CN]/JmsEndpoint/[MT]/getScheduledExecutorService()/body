{
  if (scheduledExecutorService == null) {
    scheduledExecutorService=getCamelContext().getExecutorServiceStrategy().newScheduledThreadPool(this,getEndpointUri(),DEFAULT_THREADPOOL_SIZE);
  }
  return scheduledExecutorService;
}
