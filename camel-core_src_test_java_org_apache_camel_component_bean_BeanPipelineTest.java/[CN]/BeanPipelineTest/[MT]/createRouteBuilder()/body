{
  return new RouteBuilder(){
    public void configure() throws Exception {
      from("direct:input").pipeline("bean:foo","bean:bar?methodName=usingExchange","bean:baz").to("mock:result");
    }
  }
;
}
