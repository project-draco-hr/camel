{
  return new RouteBuilder(){
    public void configure(){
      from("twitter://timeline/user?type=polling&user=brettemeyer&" + getUriTokens()).transform(body().convertToString()).to("mock:result");
    }
  }
;
}
