{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("file:src/test/data/?fileName=staff.xml&noop=true").to("xslt:com.mycompany/staff_include_classpath2.xsl").to("log:foo").to("mock:result");
    }
  }
;
}
