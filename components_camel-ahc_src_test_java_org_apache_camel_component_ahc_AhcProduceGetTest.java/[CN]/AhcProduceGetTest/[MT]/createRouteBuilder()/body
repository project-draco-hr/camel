{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("direct:start").to("ahc:http://localhost:9080/foo").to("mock:result");
      from("jetty:http://localhost:9080/foo").transform(constant("Bye World"));
    }
  }
;
}
