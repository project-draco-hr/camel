{
  return new RouteBuilder(){
    public void configure(){
      from("direct:start").marshal().xmlBeans().to("direct:marshalled");
      from("direct:marshalled").unmarshal().xmlBeans().to("mock:result");
    }
  }
;
}
