{
  setAlwaysFillLimitOrders(true);
  setValidOrderTypes(new HashSet<String>(){
{
      add(OrdType.LIMIT + "");
      add(OrdType.MARKET + "");
    }
  }
);
  setMarketQuoteProvider(new DefaultMarketQuoteProvider(10.00));
}
