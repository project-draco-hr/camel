{
  String reply=invokeMultiPartService("http://localhost:9003/CamelContext/RouterPort","in0","in1");
  assertNotNull("No response received from service",reply);
  assertTrue(reply.equals("in0 in1"));
  assertNotNull("No response received from service",reply);
  assertTrue(reply.equals("in0 in1"));
}
