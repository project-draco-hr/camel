{
  SchematronEndpoint endpoint=new SchematronEndpoint();
  Templates templates=TemplatesFactory.newInstance().newTemplates(ClassLoader.getSystemResourceAsStream("sch/schematron-1.sch"));
  endpoint.setRules(templates);
  producer=new SchematronProducer(endpoint);
}
