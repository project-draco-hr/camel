{
  if (!started.get() && !starting.get() && !stopping.get()&& !stopped.get()&& !suspending.get()&& !suspended.get()&& !shutdown.get()&& !shuttingdown.get()) {
    return false;
  }
  return !isStoppingOrStopped();
}
