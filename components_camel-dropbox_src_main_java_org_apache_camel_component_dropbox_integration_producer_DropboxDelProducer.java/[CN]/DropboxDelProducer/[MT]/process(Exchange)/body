{
  DropboxResult result=DropboxAPIFacade.getInstance(configuration.getClient()).del(configuration.getRemotePath());
  result.populateExchange(exchange);
  log.info("Deleted: " + configuration.getRemotePath());
}
