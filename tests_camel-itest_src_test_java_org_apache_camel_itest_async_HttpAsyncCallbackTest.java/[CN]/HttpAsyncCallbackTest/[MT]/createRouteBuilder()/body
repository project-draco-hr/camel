{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("jetty:http://0.0.0.0:9080/myservice").delay(1000).transform(body().prepend("Hello ")).to("mock:result");
    }
  }
;
}
