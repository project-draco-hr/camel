{
  int size=0;
  Collection<SoftReference<V>> col=(Collection<SoftReference<V>>)super.values();
  for (  SoftReference<V> ref : col) {
    if (ref.get() != null) {
      size++;
    }
  }
  return size;
}
