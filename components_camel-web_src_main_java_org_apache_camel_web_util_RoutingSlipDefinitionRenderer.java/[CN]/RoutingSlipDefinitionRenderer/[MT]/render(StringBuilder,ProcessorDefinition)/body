{
  RoutingSlipDefinition routingSlip=(RoutingSlipDefinition)processor;
  buffer.append(".").append(routingSlip.getShortName()).append("(\"").append(routingSlip.getExpression()).append("\", \"").append(routingSlip.getUriDelimiter()).append("\")");
}
