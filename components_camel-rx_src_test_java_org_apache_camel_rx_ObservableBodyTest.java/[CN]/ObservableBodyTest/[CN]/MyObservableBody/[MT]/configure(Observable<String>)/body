{
  observable.map(new Func1<String,String>(){
    @Override public String call(    String body){
      return "Hello " + body;
    }
  }
).subscribe(new Action1<String>(){
    @Override public void call(    String body){
      template.sendBody(resultEndpoint,body);
    }
  }
);
}
