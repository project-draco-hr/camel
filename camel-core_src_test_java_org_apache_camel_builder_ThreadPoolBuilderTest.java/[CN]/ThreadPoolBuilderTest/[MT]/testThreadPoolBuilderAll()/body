{
  ThreadPoolProfile profile=new ThreadPoolBuilder("myPool").poolSize(50).maxPoolSize(100).maxQueueSize(2000).keepAliveTime(20000).timeUnit(TimeUnit.MILLISECONDS).rejectedPolicy(ThreadPoolRejectedPolicy.DiscardOldest).build();
  getAndShutdown(profile);
}
