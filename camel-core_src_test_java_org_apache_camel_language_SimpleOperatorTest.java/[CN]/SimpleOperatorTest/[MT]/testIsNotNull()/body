{
  assertPredicate("${in.header.foo} != null",true);
  assertPredicate("${in.header.none} != null",false);
  assertPredicate("${in.header.foo} != 'null'",true);
  assertPredicate("${in.header.none} != 'null'",false);
}
