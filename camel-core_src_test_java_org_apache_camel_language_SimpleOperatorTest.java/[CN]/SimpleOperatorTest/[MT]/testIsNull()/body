{
  assertPredicate("${in.header.foo} == null",false);
  assertPredicate("${in.header.none} == null",true);
  assertPredicate("${in.header.foo} == 'null'",false);
  assertPredicate("${in.header.none} == 'null'",true);
}
