{
  assertExpression("${in.header.foo} == null",false);
  assertExpression("${in.header.none} == null",true);
  assertExpression("${in.header.foo} == 'null'",false);
  assertExpression("${in.header.none} == 'null'",true);
}
