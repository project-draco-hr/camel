{
  return new SpringRouteBuilder<Exchange>(){
    public void configure(){
      from(fromUri).idempotentConsumer(header("messageId"),jpaMessageIdRepository(bean(JpaTemplate.class),"myProcessorName")).to(toUri);
    }
  }
;
}
