{
  return new SpringRouteBuilder<Exchange>(){
    public void configure(){
      from(endpointUri).idempotentConsumer(header("messageId"),jpaMessageIdRepository(bean(JpaTemplate.class),"myProcessorName")).process(processor);
    }
  }
;
}
