{
  return new RouteBuilder(){
    @Override public void configure(){
      from("direct:start").to("hbase://" + DEFAULTTABLE);
      from("direct:scan").to("hbase://" + DEFAULTTABLE + "?operation="+ HBaseContats.SCAN+ "&maxResults=2&family=family1&qualifier=column1");
    }
  }
;
}
