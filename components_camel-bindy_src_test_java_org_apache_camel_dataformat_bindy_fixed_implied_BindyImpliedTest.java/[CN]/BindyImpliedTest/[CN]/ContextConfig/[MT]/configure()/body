{
  from(URI_DIRECT_MARSHALL).marshal().bindy(BindyType.Fixed,Record.class).to(URI_MOCK_MARSHALL_RESULT);
  from(URI_DIRECT_UNMARSHALL).unmarshal().bindy(BindyType.Fixed,Record.class).to(URI_MOCK_UNMARSHALL_RESULT);
}
