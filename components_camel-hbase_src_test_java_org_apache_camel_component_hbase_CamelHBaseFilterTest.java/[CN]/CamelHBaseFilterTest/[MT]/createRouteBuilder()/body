{
  return new RouteBuilder(){
    @Override public void configure(){
      from("direct:start").to("hbase://" + DEFAULTTABLE);
      from("direct:scan").to("hbase://" + DEFAULTTABLE + "?operation="+ HBaseConstants.SCAN+ "&maxResults=2&filters=#myFilters");
    }
  }
;
}
