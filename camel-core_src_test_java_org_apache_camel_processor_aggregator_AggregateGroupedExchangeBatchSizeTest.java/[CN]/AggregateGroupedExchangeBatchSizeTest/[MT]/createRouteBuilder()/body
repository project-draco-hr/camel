{
  return new RouteBuilder(){
    public void configure() throws Exception {
      from("direct:start").aggregate().constant(true).batchSize(2).batchTimeout(500L).groupExchanges().to("mock:result");
    }
  }
;
}
