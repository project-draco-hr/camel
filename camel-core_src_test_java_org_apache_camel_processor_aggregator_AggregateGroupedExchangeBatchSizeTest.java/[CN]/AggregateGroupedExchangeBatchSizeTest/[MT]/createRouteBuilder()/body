{
  return new RouteBuilder(){
    public void configure() throws Exception {
      from("direct:start").aggregate().constant(true).completionSize(2).completionTimeout(500L).groupExchanges().to("mock:result");
    }
  }
;
}
