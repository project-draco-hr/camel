{
  try {
    Student student=(Student)obj;
    return stuLastName.equals(student.stuLastName) && stuFirstName.equals(student.stuFirstName) && stuAge.equals(student.stuAge);
  }
 catch (  ClassCastException e) {
    return false;
  }
}
