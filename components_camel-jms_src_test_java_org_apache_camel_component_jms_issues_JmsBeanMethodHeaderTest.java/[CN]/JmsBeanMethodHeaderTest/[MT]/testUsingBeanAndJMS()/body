{
  MockEndpoint mock=getMockEndpoint("mock:approve");
  mock.expectedBodiesReceived("Yes");
  template.sendBodyAndHeader("activemq:approve","James",BeanProcessor.METHOD_NAME,"approveLoan");
  mock.assertIsSatisfied();
}
