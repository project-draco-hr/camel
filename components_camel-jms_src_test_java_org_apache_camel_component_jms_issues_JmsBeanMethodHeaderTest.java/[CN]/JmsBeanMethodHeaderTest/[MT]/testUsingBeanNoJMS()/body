{
  MockEndpoint mock=getMockEndpoint("mock:approve");
  mock.expectedBodiesReceived("Yes");
  template.sendBodyAndHeader("direct:approve",ExchangePattern.InOut,"James",BeanProcessor.METHOD_NAME,"approveLoan");
  mock.assertIsSatisfied();
}
