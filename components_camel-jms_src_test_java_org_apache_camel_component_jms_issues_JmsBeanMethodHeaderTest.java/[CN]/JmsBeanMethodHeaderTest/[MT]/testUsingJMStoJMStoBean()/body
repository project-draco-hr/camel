{
  MockEndpoint mock=getMockEndpoint("mock:approve");
  mock.expectedBodiesReceived("No");
  template.sendBodyAndHeader("activemq:queue","James",BeanProcessor.METHOD_NAME,"approveSuperLoan");
  mock.assertIsSatisfied();
}
