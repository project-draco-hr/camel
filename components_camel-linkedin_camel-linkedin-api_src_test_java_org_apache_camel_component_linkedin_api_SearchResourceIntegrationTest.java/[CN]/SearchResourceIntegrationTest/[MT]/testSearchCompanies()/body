{
  execute(new Runnable(){
    @Override public void run(){
      final CompanySearch companySearch=searchResource.searchCompanies(DEFAULT_FIELDS,"linkedin",null,null,null,null,null,null);
      assertNotNull(companySearch);
    }
  }
);
}
