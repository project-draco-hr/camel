{
  return new RouteBuilder(){
    public void configure(){
      from("twitter://timeline/public?type=polling&" + getUriTokens()).transform(body().convertToString()).to("mock:result");
    }
  }
;
}
