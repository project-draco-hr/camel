{
  Endpoint target=endpoint != null ? endpoint : defaultEndpoint;
  return exchangeSupplier != null ? template().asyncSend(target,exchangeSupplier.get()) : template().asyncSend(target,processorSupplier.get());
}
