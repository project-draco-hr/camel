{
  MockEndpoint mock=getMockEndpoint("mock:report");
  mock.expectedBodiesReceived("Hello Not Locked");
  template.sendBodyAndHeader("newfile:target/reports/notlocked","Hello Not Locked",NewFileComponent.HEADER_FILE_NAME,"report.txt");
  mock.assertIsSatisfied();
  Thread.sleep(200);
  checkLockFile(false);
}
