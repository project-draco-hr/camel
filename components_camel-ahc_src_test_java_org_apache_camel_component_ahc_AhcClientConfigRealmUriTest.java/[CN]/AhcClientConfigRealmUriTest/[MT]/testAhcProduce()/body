{
  Object out=template.requestBody(getAhcEndpointUri() + "?realm.scheme=DIGEST&realm.principal=someUser" + "&realm.password=somePass&realm.usePreemptiveAuth=true",null,String.class);
  assertEquals("Bye World",out);
}
