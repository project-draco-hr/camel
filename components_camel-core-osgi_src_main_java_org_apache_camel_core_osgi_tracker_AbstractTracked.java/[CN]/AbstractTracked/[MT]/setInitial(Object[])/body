{
  if (list == null) {
    return;
  }
  int size=list.length;
  for (int i=0; i < size; i++) {
    Object item=list[i];
    if (item == null) {
      continue;
    }
    if (DEBUG) {
      System.out.println("AbstractTracked.setInitial: " + item);
    }
    initial.add(item);
  }
}
