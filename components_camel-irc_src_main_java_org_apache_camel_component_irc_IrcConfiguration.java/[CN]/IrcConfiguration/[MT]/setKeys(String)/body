{
  if (channels.size() == 0) {
    channelKeys=keys;
  }
 else {
    String[] s=keys.split(",");
    int index=0;
    for (    String key : s) {
      this.keys.put(channels.get(index),key);
      index++;
    }
  }
}
