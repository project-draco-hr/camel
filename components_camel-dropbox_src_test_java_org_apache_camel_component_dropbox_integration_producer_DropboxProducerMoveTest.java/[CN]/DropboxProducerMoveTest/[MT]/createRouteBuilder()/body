{
  return new RouteBuilder(){
    public void configure(){
      from("direct:start").to("dropbox://move?" + getAuthParams() + "&remotePath=/XXX&newRemotePath=/XXX").to("mock:result");
    }
  }
;
}
