{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("file:target/pair").split().tokenizeXML("order","orders").to("mock:split");
      from("file:target/pair2").split(body().tokenizeXML("order","orders")).to("mock:split");
    }
  }
;
}
