{
  return new RouteBuilder(){
    public void configure(){
      from("jetty:http://localhost:8192/test").setBody(body(String.class)).to("log:in").tryBlock().to("validator:OptimizationRequest.xsd").setBody(constant("<ok/>")).handle(ValidationException.class).setBody(constant("<error/>")).end().to("log:out");
    }
  }
;
}
