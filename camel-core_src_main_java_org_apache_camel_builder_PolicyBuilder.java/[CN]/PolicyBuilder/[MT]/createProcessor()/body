{
  if (target == null)   throw new RuntimeCamelException("target not provided.");
  Processor<E> last=target.createProcessor();
  Collections.reverse(policies);
  for (  Policy<E> p : policies) {
    last=p.wrap(last);
  }
  return last;
}
