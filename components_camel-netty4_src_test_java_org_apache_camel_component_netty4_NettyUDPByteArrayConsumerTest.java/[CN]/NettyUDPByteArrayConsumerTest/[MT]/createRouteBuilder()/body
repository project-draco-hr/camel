{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("netty4:udp://localhost:{{port}}?sync=false&udpByteArrayCodec=true").to("mock:result");
    }
  }
;
}
