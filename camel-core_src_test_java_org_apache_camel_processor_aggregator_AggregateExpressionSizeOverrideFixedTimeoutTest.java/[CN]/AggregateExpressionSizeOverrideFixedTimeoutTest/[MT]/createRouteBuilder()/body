{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("direct:start").aggregate(header("id"),new BodyInAggregatingStrategy()).completionSize(2).completionSize(header("mySize")).completionTimeout(1000).to("mock:aggregated");
    }
  }
;
}
