{
  return new RouteBuilder(){
    Namespaces ns=new Namespaces("C","urn:c");
    public void configure(){
      from("direct:start").split().tokenizeXML("//C:child",true,ns).to("mock:result").end();
    }
  }
;
}
