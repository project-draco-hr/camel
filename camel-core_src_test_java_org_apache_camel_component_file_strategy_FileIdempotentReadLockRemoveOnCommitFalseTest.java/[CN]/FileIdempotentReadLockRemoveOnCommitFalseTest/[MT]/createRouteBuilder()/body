{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("file:target/changed/in?readLock=idempotent&idempotentRepository=#myRepo&readLockRemoveOnCommit=false").to("mock:result");
    }
  }
;
}
