{
  return new RouteBuilder(){
    public void configure(){
      from("twitter://search?type=polling&keywords=java&" + getUriTokens()).transform(body().convertToString()).to("mock:result");
    }
  }
;
}
