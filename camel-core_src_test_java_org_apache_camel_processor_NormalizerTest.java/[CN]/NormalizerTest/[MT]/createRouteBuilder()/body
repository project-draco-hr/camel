{
  return new RouteBuilder(){
    public void configure(){
      from("direct:start").choice().when().xpath("/employee").to("bean:normalizer?method=employeeToPerson").to("mock:result").when().xpath("/customer").to("bean:normalizer?method=customerToPerson").to("mock:result");
    }
  }
;
}
