{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("direct:start").to("bean:myBean?methodName=b").bean(beanMock,"a").beanRef("myBean","c");
    }
  }
;
}
