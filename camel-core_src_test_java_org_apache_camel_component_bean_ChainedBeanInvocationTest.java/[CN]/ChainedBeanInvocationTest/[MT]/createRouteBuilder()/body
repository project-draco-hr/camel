{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("direct:start").to("bean:myBean?methodName=a").bean(beanMock,"b").beanRef("myBean","c");
    }
  }
;
}
