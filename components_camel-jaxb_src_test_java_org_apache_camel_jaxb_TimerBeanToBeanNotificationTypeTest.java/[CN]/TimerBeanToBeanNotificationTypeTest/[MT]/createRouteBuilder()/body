{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("timer:foo?repeatCount=1").log("Timer triggered").bean(MyNotificationService.class,"createNotification").bean(MyNotificationService.class,"sendNotification");
    }
  }
;
}
