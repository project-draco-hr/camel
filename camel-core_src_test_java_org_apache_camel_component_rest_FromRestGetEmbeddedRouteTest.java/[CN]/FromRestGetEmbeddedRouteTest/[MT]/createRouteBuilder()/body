{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      rest().path("/say/hello").get().to("mock:hello").transform(constant("Hello World")).endPath().path("/say/bye").get().accept("application/json").to("mock:bye").transform(constant("Bye World")).post().to("mock:update");
    }
  }
;
}
