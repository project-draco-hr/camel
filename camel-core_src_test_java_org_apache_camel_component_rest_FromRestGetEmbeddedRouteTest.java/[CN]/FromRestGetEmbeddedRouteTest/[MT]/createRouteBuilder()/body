{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      rest("/say/hello").get().to("mock:hello").transform(constant("Hello World"));
      rest("/say/bye").get().consumes("application/json").to("mock:bye").transform(constant("Bye World")).post().to("mock:update");
    }
  }
;
}
