{
  if (leader.get()) {
    if (shouldStopConsumer) {
      startConsumer(route);
    }
  }
 else {
    if (shouldStopConsumer) {
      stopConsumer(route);
    }
    exchange.setException(new IllegalStateException("Hazelcast based route policy prohibits processing exchanges, stopping route and failing the exchange"));
  }
}
