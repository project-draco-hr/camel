{
  initResequencer(1,capacity);
  for (int i=99; i >= 0; i--) {
    resequencer.insert(i);
  }
  System.out.println("\nOutput sequence:");
  for (int i=0; i < 100; i++) {
    System.out.print(buffer.take() + " ");
  }
}
