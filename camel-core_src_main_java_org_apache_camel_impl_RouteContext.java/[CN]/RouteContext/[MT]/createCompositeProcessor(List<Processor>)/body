{
  if (list.size() == 0) {
    return null;
  }
  Processor processor;
  if (list.size() == 1) {
    processor=list.get(0);
  }
 else {
    processor=new MulticastProcessor(list);
  }
  return processor;
}
