{
  return new RouteBuilder(){
    public void configure() throws Exception {
      errorHandler(deadLetterChannel("mock:error").maximumRedeliveries(1));
      from("direct:start").onException(OrderFailedException.class).handled(true).bean(OrderService.class,"orderFailed").to("mock:error").end().bean(OrderService.class,"handleOrder").to("mock:result");
    }
  }
;
}
