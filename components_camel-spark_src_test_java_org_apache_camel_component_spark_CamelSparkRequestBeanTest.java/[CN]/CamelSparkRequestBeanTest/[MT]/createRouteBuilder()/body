{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("spark:get:/hello/*/to/*").to("mock:foo").bean(CamelSparkRequestBeanTest.class,"doSomething");
    }
  }
;
}
