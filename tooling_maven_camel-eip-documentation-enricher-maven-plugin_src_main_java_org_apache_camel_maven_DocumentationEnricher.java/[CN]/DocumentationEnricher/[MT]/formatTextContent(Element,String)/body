{
  StringBuilder stringBuilder=new StringBuilder();
  stringBuilder.append(System.lineSeparator()).append(WordUtils.wrap(textContent,Constants.WRAP_LENGTH)).append(System.lineSeparator()).append(StringUtils.repeat(Constants.DEFAULT_XML_INTEMSION,getNodeDepth(item)));
  return stringBuilder.toString();
}
