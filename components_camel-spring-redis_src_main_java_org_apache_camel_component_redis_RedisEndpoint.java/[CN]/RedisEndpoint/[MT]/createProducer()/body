{
  Command defaultCommand=configuration.getCommand();
  if (defaultCommand == null) {
    defaultCommand=Command.SET;
  }
  return new RedisProducer(this,new RedisClient(configuration.getRedisTemplate()),RedisConstants.COMMAND,defaultCommand.name(),((RedisComponent)getComponent()).getExchangeConverter());
}
