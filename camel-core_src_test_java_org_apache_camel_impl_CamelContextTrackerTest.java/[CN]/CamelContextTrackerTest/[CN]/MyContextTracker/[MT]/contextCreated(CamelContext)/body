{
  camelContext.addLifecycleStrategy(new LifecycleStrategySupport(){
    @Override public void onContextStop(    CamelContext context){
      names.remove(context.getName());
    }
  }
);
  names.add(camelContext.getName());
}
