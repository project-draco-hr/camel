{
  context.addRoutes(new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("sjmsbatch:in?aggregationStrategy=#aggStrategy&consumerCount=-1").to("mock:out");
    }
  }
);
  context.start();
}
