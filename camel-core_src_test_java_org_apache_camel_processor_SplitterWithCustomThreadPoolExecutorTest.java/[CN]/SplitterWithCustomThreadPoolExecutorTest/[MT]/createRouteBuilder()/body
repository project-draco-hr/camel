{
  return new RouteBuilder(){
    public void configure(){
      from("direct:parallel-custom-pool").split(body().tokenize(",")).parallelProcessing(true).executor(customThreadPoolExecutor).to("mock:result");
    }
  }
;
}
