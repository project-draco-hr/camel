{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("sftp://localhost:" + getPort() + "/?username=admin&password=admin&delay=10s&disconnect=true&recursive=true&stepwise=false").routeId("foo").noAutoStartup().to("log:result","mock:result");
    }
  }
;
}
