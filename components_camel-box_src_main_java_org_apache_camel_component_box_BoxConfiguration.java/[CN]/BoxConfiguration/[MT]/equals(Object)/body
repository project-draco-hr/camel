{
  if (obj instanceof BoxConfiguration) {
    final BoxConfiguration other=(BoxConfiguration)obj;
    return boxConfig == other.boxConfig && connectionManagerBuilder == other.connectionManagerBuilder && httpParams == other.httpParams && clientId == other.clientId && clientSecret == other.clientSecret && authSecureStorage == other.authSecureStorage;
  }
  return false;
}
