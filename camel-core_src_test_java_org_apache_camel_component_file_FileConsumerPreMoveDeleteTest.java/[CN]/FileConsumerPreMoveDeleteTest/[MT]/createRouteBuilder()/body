{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("file://target/premove?preMove=work&delete=true&idempotent=false").process(new MyPreMoveCheckerProcessor()).to("mock:result");
    }
  }
;
}
