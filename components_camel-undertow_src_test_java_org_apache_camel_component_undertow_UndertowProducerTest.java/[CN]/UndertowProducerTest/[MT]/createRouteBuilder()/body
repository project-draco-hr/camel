{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("undertow:http://localhost:8888/foo").to("mock:input").transform().constant("Bye World");
    }
  }
;
}
