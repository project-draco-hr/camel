{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("seda:start").aggregate(header("id"),new UseLatestAggregationStrategy()).completionInterval(5000).to("mock:result");
    }
  }
;
}
