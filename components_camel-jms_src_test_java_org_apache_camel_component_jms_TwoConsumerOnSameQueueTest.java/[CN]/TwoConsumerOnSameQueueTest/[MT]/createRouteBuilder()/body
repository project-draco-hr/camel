{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("activemq:queue:foo").routeId("a").to("mock:a");
      from("activemq:queue:foo").routeId("b").to("mock:b");
    }
  }
;
}
