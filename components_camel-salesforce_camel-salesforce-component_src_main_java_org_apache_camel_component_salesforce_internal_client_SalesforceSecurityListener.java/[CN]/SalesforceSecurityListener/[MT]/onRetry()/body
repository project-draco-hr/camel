{
  if (retrying) {
    retrying=false;
    retries++;
    setDelegatingRequests(true);
    setDelegatingResponses(true);
    requestComplete=false;
    responseComplete=false;
  }
  super.onRetry();
}
