{
  if (isLenientProperties()) {
    String uri=getEndpointUri();
    if (uri.indexOf('?') != -1) {
      return ObjectHelper.before(uri,"?");
    }
 else {
      return uri;
    }
  }
 else {
    return getEndpointUri();
  }
}
