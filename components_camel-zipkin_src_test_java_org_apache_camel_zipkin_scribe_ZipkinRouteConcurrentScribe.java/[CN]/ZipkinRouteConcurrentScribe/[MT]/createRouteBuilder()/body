{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("direct:start").to("sed:foo");
      from("seda:foo?concurrentConsumers=2").routeId("foo").log("routing at ${routeId}").delay(simple("${random(1000,2000)}")).to("seda:bar");
      from("seda:bar?concurrentConsumers=2").routeId("bar").log("routing at ${routeId}").delay(simple("${random(0,500)}"));
    }
  }
;
}
