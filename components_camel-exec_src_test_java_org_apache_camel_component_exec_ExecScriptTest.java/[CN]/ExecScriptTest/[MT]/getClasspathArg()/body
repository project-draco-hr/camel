{
  String classpath=System.getProperty("java.class.path");
  if (OS.isFamilyWindows()) {
    classpath="\"\"" + classpath + "\"\"";
  }
 else {
    classpath="\"" + classpath + "\"";
  }
  return classpath;
}
