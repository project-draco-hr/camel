{
  Exchange exchange=endpoint.createExchange();
  KeyFormatStrategy initialisedKeyFormatStrategy=(keyFormatStrategy == null) ? new DefaultJmsKeyFormatStrategy() : keyFormatStrategy;
  return populateExchange(message,exchange,false,initialisedKeyFormatStrategy);
}
