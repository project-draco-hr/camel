{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("direct:start").to("sqs://MyQueue?amazonSQSClient=#amazonSQSClient");
      from("sqs://MyQueue?amazonSQSClient=#amazonSQSClient").to("mock:result");
    }
  }
;
}
