{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      SoapJaxbDataFormat df=createDataFormat();
      from("direct:start").onException(Exception.class).marshal(df).end().marshal(df).to("mock:result");
    }
  }
;
}
