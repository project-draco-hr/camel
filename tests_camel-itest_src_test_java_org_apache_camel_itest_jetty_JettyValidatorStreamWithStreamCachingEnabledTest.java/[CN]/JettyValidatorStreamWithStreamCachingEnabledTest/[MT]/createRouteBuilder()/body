{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      context.setStreamCaching(true);
      from("jetty:http://localhost:8192/test").to("validator:OptimizationRequest.xsd").transform(constant("<ok/>"));
    }
  }
;
}
