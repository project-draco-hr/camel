{
  return new RouteBuilder(){
    @Override public void configure(){
      from("direct:start").errorHandler(deadLetterChannel("mock:error").maximumRedeliveries(4).loggingLevel(LoggingLevel.DEBUG)).to("mock:a").handleFault().process(successOnRetryProcessor).to("mock:b");
    }
  }
;
}
