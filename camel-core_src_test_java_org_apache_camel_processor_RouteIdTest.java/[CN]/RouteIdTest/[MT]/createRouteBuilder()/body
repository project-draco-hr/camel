{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("direct:start").id("myCoolRoute").onException(Exception.class).to("mock:error").end().to("mock:result");
    }
  }
;
}
