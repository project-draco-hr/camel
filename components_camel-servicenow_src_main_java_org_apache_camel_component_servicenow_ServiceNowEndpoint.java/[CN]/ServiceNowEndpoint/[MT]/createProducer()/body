{
  ServiceNowProducer producer=configuration.getRelease().get(this);
  LOGGER.info("Producer for ServiceNow Rel. {} = {}/{}",configuration.getRelease().name(),producer.getRelease().name(),producer.getClass().getName());
  return producer;
}
