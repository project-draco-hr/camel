{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("direct:start").to("ahc:http://localhost:{{port}}/foo").to("mock:result");
      from("jetty:http://localhost:{{port}}/foo").transform(simple("Bye ${body}"));
    }
  }
;
}
