{
  if (unsubscribed.compareAndSet(false,true)) {
    if (consumer != null) {
      try {
        ServiceHelper.stopServices(consumer);
        observer.onCompleted();
      }
 catch (      Exception e) {
        observer.onError(e);
      }
    }
  }
}
