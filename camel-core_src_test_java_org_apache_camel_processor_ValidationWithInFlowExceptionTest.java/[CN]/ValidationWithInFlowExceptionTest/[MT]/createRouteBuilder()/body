{
  return new RouteBuilder(){
    public void configure(){
      onException(ValidationException.class).to("mock:invalid");
      from("direct:start").onException(ValidationException.class).to("mock:invalid").end().process(validator).to("mock:valid");
    }
  }
;
}
