{
  return new RouteBuilder(){
    public void configure(){
      exception(ValidationException.class).to("mock:invalid");
      from("direct:start").exception(ValidationException.class).to("mock:invalid").end().process(validator).to("mock:valid");
    }
  }
;
}
