{
  return new RouteBuilder(){
    public void configure(){
      from("direct:start").to("log:foo").wireTap("direct:tap").to("mock:result");
      from("direct:tap").delay(100).setBody().constant("Tapped").to("mock:result","mock:tap");
    }
  }
;
}
