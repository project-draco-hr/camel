{
  LOG.debug("Stopping");
  try {
    disconnect();
  }
 catch (  Exception e) {
    LOG.debug("Exception occured during disconnecting from " + remoteServer() + " "+ e.getMessage());
  }
  super.doStop();
}
