{
  return new RouteBuilder(){
    public void configure(){
      from("direct:a").setHeader("esc",constant(new EscapeTool())).to("velocity:org/apache/camel/component/velocity/escape.vm");
    }
  }
;
}
