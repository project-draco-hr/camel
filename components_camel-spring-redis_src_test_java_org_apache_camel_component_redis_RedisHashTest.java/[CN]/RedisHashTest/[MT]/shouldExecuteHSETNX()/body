{
  when(hashOperations.putIfAbsent(anyString(),anyString(),anyString())).thenReturn(true);
  Object result=sendHeaders(RedisConstants.COMMAND,"HSETNX",RedisConstants.KEY,"key",RedisConstants.FIELD,"field",RedisConstants.VALUE,"value");
  verify(hashOperations).putIfAbsent("key","field","value");
  assertEquals(true,result);
}
