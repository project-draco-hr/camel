{
synchronized (this) {
    if (websocket == null) {
      try {
        connect();
      }
 catch (      Exception e) {
        LOG.error("Failed to connect",e);
      }
    }
  }
  return websocket;
}
