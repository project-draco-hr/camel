{
  return new RouteBuilder(){
    public void configure(){
      from("mqtt:bar?subscribeTopicNames=" + TEST_TOPICS_WITH_WILDCARDS).transform(body().convertToString()).to("mock:result");
    }
  }
;
}
