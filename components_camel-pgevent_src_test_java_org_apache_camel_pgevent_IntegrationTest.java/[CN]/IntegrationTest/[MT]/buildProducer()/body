{
  RouteBuilder builder=new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("timer://test?fixedRate=true&period=5000").to("pgevent://127.0.0.1:5432/event_tests/testchannel?user=dphillips");
    }
  }
;
  return builder;
}
