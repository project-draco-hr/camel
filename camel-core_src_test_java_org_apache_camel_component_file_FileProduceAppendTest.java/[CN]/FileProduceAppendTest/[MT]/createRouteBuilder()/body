{
  return new RouteBuilder(){
    public void configure(){
      from("direct:start").setHeader(FileComponent.HEADER_FILE_NAME,constant("hello.txt")).to("file://target/test-file-append?append=true","mock:result");
    }
  }
;
}
