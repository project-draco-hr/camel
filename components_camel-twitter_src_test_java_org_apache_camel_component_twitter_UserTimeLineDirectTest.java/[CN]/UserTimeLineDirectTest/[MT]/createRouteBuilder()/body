{
  return new RouteBuilder(){
    public void configure(){
      from("twitter://timeline/user?type=direct&user=brettemeyer&" + getUriTokens()).transform(body().convertToString()).to("mock:result");
    }
  }
;
}
