{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("direct:start").to("ahc:http://localhost:9080/foo?transferException=true").to("mock:result");
      from("jetty:http://localhost:9080/foo?transferException=true").throwException(new MyOrderException("123"));
    }
  }
;
}
