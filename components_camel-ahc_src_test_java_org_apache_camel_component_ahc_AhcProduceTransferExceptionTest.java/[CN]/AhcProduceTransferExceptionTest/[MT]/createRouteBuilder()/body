{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("direct:start").to("ahc:http://localhost:{{port}}/foo?transferException=true").to("mock:result");
      from("jetty:http://localhost:{{port}}/foo?transferException=true").throwException(new MyOrderException("123"));
    }
  }
;
}
