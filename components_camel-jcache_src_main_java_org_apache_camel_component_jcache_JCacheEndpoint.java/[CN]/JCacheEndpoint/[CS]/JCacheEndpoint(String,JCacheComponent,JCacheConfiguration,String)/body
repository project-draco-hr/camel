{
  super(uri,component);
  this.cacheName=cacheName;
  this.cacheCnfiguration=configuration;
  this.cacheManager=new JCacheManager<>(configuration,cacheName,getCamelContext().getApplicationContextClassLoader(),super.getCamelContext());
}
