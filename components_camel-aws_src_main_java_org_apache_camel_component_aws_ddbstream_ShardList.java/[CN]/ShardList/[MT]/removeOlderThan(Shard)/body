{
  String current=removeBefore.getParentShardId();
  while (current != null) {
    Shard s=shards.remove(current);
    if (s == null) {
      current=null;
    }
 else {
      current=s.getParentShardId();
    }
  }
}
