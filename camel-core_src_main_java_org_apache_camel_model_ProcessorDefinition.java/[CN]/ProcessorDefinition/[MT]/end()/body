{
  ProcessorDefinition defn=(ProcessorDefinition)this;
  if (defn instanceof TryDefinition) {
    popBlock();
  }
  if (blocks.isEmpty()) {
    if (parent == null) {
      return this;
    }
    return parent;
  }
  popBlock();
  return this;
}
