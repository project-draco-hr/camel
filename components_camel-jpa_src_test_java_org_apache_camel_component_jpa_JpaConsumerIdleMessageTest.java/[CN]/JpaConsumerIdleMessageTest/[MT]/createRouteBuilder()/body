{
  return new RouteBuilder(){
    public void configure() throws Exception {
      from("jpa://org.apache.camel.examples.MultiSteps?consumer.nativeQuery=select * from MultiSteps where step = 999" + "&delay=100&sendEmptyMessageWhenIdle=true").to("mock:result");
    }
  }
;
}
