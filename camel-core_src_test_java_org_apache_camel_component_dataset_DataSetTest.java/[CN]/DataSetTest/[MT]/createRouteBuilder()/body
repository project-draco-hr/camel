{
  return new RouteBuilder(){
    public void configure() throws Exception {
      from("dataset:foo").multicast().to("mock:results").to("seda:foo");
      from("seda:foo").to("dataset:foo");
    }
  }
;
}
