{
  Jt400Endpoint endpoint=resolveMandatoryEndpoint("jt400://user:" + PASSWORD + "@host/qsys.lib/library.lib/queue.dtaq?connectionPool=#mockPool&pollStrategy=#jt400PollStrategy",Jt400Endpoint.class);
  assertNotNull(endpoint);
  PollingConsumer consumer=endpoint.createPollingConsumer();
  assertNotNull(consumer);
}
