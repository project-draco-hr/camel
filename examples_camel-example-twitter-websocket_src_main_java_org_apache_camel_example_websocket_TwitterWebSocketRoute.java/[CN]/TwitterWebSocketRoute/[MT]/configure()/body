{
  WebsocketComponent wc=getContext().getComponent("websocket",WebsocketComponent.class);
  wc.setPort(port);
  wc.setStaticResources("classpath:.");
  TwitterComponent tc=getContext().getComponent("twitter",TwitterComponent.class);
  tc.setAccessToken(accessToken);
  tc.setAccessTokenSecret(accessTokenSecret);
  tc.setConsumerKey(consumerKey);
  tc.setConsumerSecret(consumerSecret);
  fromF("twitter://search?type=polling&delay=%s&keywords=%s",delay,searchTerm).to("log:tweet").to("websocket:camel-tweet?sendToAll=true");
}
