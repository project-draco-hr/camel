{
  WebsocketComponent wc=getContext().getComponent("websocket",WebsocketComponent.class);
  wc.setPort(port);
  wc.setStaticResources("classpath:.");
  fromF("twitter://search?type=polling&delay=%s&keywords=%s",delay,searchTerm).to("websocket:camel-tweet?sendToAll=true");
}
