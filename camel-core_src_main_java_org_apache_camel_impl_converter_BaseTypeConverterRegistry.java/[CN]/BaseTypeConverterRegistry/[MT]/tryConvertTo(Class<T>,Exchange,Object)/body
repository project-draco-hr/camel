{
  if (!isRunAllowed()) {
    return null;
  }
  Object answer;
  try {
    answer=doConvertTo(type,exchange,value,true);
  }
 catch (  Exception e) {
    return null;
  }
  if (answer == Void.TYPE) {
    return null;
  }
 else {
    return (T)answer;
  }
}
