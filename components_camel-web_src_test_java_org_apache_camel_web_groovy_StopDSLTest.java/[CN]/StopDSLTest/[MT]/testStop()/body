{
  String DSL="from(\"direct:start\").choice().when(body().contains(\"Hello\")).to(\"mock:hello\").when(body().contains(\"Bye\")).to(\"mock:bye\").stop()" + ".otherwise().to(\"mock:other\").end().to(\"mock:result\")";
  String expectedDSL=DSL;
  assertEquals(expectedDSL,render(DSL));
}
