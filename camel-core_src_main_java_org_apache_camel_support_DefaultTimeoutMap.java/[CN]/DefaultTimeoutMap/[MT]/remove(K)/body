{
  TimeoutMapEntry<K,V> entry;
  if (useLock) {
    lock.lock();
  }
  try {
    entry=map.remove(id);
  }
  finally {
    if (useLock) {
      lock.unlock();
    }
  }
  return entry != null ? entry.getValue() : null;
}
