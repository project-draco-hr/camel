{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("direct:start").hystrixCircuitBreaker().throwException(new IllegalArgumentException("Forced")).fallback().transform().constant("Fallback message").end().to("mock:result");
    }
  }
;
}
