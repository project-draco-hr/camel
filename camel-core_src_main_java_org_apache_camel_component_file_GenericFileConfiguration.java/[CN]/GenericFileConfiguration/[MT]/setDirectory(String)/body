{
  this.directory=needToNormalize() ? FileUtil.normalizePath(directory) : directory;
  if (this.directory == null) {
    this.directory="";
  }
  this.directory=FileUtil.stripTrailingSeparator(this.directory);
}
