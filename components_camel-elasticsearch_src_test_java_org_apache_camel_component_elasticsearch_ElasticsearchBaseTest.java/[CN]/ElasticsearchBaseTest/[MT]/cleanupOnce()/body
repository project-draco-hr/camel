{
  deleteDirectory("target/data");
  node=nodeBuilder().local(true).settings(ImmutableSettings.settingsBuilder().put("http.enabled",false).put("path.data","target/data")).node();
  client=node.client();
}
