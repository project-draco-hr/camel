{
  final String sqsURI=String.format("aws-sqs://MyQueue?accessKey=%s&secretKey=%s&messageRetentionPeriod=%s&maximumMessageSize=%s&policy=%s",accessKey,secretKey,"1209600","65536","");
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("direct:start").to(sqsURI);
      from(sqsURI).to("mock:result");
    }
  }
;
}
