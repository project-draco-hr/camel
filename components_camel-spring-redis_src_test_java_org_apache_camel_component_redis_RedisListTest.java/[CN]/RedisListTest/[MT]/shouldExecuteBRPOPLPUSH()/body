{
  when(listOperations.rightPopAndLeftPush(anyString(),anyString(),anyLong(),any(TimeUnit.class))).thenReturn("value");
  Object result=sendHeaders(RedisConstants.COMMAND,"BRPOPLPUSH",RedisConstants.KEY,"key",RedisConstants.DESTINATION,"destination",RedisConstants.TIMEOUT,"10");
  verify(listOperations).rightPopAndLeftPush("key","destination",10,TimeUnit.SECONDS);
  assertEquals("value",result);
}
