{
  return new RouteBuilder(){
    public void configure() throws Exception {
      from("direct:start").aggregate(constant(true)).groupExchanges().completionSize(3).to("mock:result").end();
    }
  }
;
}
