{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("sjms:topic:foo?durableSubscriptionId=bar").to("mock:result");
      from("sjms:topic:foo?durableSubscriptionId=bar").to("mock:result2");
    }
  }
;
}
