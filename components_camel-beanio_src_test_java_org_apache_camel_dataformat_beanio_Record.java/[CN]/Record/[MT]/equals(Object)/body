{
  if (obj == null) {
    return false;
  }
 else   if (obj == this) {
    return true;
  }
 else {
    Record record=(Record)obj;
    return sedol.equals(record.getSedol()) && source.equals(record.getSource());
  }
}
