{
  return new RouteBuilder(){
    public void configure(){
      from("direct:start").filter(xpath("/c:person[@name='James']").namespace("c","http://acme.com/cheese")).to("mock:result");
    }
  }
;
}
