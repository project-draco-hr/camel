{
  JndiContext answer=new JndiContext();
  answer.bind("myCustomManager",new SSLDefaultTrustManager(){
    @Override public boolean isTrusted(    X509Certificate[] chain){
      return true;
    }
    @Override public String toString(){
      return "MyCustomManager!";
    }
  }
);
  return answer;
}
