{
  final ContentExchange get=getContentExchange(HttpMethods.GET,sobjectsUrl(sObjectName + "/" + id+ "/"+ blobFieldName));
  setAccessToken(get);
  doHttpRequest(get,new DelegatingClientCallback(callback));
}
