{
  final ContentExchange delete=getContentExchange(HttpMethods.DELETE,sobjectsExternalIdUrl(sObjectName,fieldName,fieldValue));
  setAccessToken(delete);
  doHttpRequest(delete,new DelegatingClientCallback(callback));
}
