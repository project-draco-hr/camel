{
  if (term == null) {
    term=newTerm;
  }
 else   if (op == Op.and) {
    term=new AndTerm(term,newTerm);
  }
 else   if (op == Op.or) {
    term=new OrTerm(term,newTerm);
  }
 else {
    term=new AndTerm(term,new NotTerm(newTerm));
  }
}
