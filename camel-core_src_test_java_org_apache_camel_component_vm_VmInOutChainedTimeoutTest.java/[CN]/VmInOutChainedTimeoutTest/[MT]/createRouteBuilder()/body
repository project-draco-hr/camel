{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      errorHandler(noErrorHandler());
      from("vm:a").to("mock:a").to("vm:b?timeout=2000").to("mock:a2");
      from("vm:b").to("mock:b").delay(3000).transform().constant("Bye World");
    }
  }
;
}
