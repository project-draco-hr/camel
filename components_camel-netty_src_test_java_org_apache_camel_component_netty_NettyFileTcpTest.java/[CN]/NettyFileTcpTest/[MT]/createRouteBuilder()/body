{
  return new RouteBuilder(){
    public void configure(){
      from("netty:tcp://localhost:9123?sync=false&textline=true").to("mock:results");
      from("file:src/test/data?noop=true&fileName=message1.txt").to("netty:tcp://localhost:9123?sync=false&textline=true");
    }
  }
;
}
