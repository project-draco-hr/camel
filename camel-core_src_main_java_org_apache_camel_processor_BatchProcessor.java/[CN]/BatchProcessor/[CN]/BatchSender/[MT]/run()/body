{
  while (true) {
    try {
      Thread.sleep(batchTimeout);
    }
 catch (    InterruptedException e) {
      if (cancelRequested) {
        return;
      }
    }
    try {
      sendExchanges();
    }
 catch (    Exception e) {
      getExceptionHandler().handleException(e);
    }
  }
}
