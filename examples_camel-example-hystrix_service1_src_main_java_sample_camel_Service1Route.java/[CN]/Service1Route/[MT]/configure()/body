{
  from("jetty:http://0.0.0.0:{{service1.port}}/service1").routeId("service1").streamCaching().log("Service1 request: ${body}").transform(simple("Service1-${body}")).log("Service1 response: ${body}");
}
