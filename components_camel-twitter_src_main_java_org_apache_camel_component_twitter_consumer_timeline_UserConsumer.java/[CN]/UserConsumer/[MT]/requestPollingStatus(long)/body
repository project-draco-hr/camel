{
  List<twitter4j.Status> statusList=te.getTwitter().getUserTimeline(te.getProperties().getUser(),new Paging(lastStatusUpdateId));
  return TwitterConverter.convertStatuses(statusList).iterator();
}
