{
  if (defaultHandler != null) {
    throw new IllegalArgumentException(String.format("Duplicate default handler: '%s', '%s'",defaultHandler,handler));
  }
  defaultHandler=handler;
  handlerString=null;
}
