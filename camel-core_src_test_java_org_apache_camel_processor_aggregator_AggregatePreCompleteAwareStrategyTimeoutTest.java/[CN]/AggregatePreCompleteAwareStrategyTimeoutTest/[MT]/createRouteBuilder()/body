{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("direct:start").aggregate(header("id"),new BodyInPreCompleteAggregatingStrategy()).completionTimeout(1000).to("mock:aggregated");
    }
  }
;
}
