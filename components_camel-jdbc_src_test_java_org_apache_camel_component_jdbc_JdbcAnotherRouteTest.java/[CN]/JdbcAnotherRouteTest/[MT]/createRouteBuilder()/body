{
  return new RouteBuilder(){
    public void configure() throws Exception {
      from("timer://kickoff?period=10000").setBody(constant("select * from customer")).to("jdbc:testdb").to("mock:result");
    }
  }
;
}
