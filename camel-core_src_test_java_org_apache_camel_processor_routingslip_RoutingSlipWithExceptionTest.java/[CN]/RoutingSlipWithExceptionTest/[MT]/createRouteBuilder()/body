{
  return new RouteBuilder(){
    public void configure(){
      from("direct:start").tryBlock().routingSlip(ROUTING_SLIP_HEADER).to("mock:noexception").handle(Exception.class).to("mock:exception");
    }
  }
;
}
