{
  if (msg instanceof byte[]) {
    byte[] bytes=(byte[])msg;
    ByteBuf buf=ByteBufAllocator.DEFAULT.buffer(bytes.length);
    buf.writeBytes(bytes);
    out.add(buf);
  }
 else {
    out.add(msg);
  }
}
