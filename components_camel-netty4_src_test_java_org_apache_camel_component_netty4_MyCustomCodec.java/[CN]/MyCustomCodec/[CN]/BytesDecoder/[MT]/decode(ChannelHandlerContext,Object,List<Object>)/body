{
  if (!(msg instanceof ByteBuf)) {
    out.add(msg);
  }
 else {
    ByteBuf cb=(ByteBuf)msg;
    if (cb.hasArray() && cb.isReadable()) {
      out.add(cb.array());
    }
 else {
      out.add((Object)null);
    }
  }
}
