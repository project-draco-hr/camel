{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("sftp://localhost:" + getPort() + "//"+ createAbsolutePath()+ "?username=admin&password=admin&delay=10s&disconnect=true&stepwise=false").routeId("foo").noAutoStartup().to("mock:result");
    }
  }
;
}
