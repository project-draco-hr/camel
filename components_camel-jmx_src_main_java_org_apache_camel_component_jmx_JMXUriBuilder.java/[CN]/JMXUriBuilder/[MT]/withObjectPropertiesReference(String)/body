{
  if (aReferenceToHashtable.startsWith("#")) {
    addProperty("objectProperties",aReferenceToHashtable);
  }
 else {
    addProperty("objectProperties","#" + aReferenceToHashtable);
  }
  return this;
}
