{
  super.doStart();
  if (chat == null) {
    chat=new MultiUserChat(endpoint.getConnection(),room);
    String nickname=this.endpoint.getNickname();
    if (nickname == null)     nickname=this.endpoint.getUser();
    chat.join(nickname);
  }
}
