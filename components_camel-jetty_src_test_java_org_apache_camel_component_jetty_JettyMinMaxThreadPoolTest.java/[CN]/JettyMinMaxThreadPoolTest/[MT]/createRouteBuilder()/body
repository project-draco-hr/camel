{
  return new RouteBuilder(){
    public void configure() throws Exception {
      from("jetty:http://localhost:{{port}}/myapp/myservice").to("log:input").delay(1000).transform(body().prepend("Bye ")).to("log:output");
    }
  }
;
}
