{
  context.addRoutes(new SpringRouteBuilder(){
    @Override public void configure() throws Exception {
      from("activemq:queue:foo").process(new ConditionalExceptionProcessor()).to("activemq:queue:bar");
    }
  }
);
  assertResult();
}
