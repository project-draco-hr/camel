{
  return new RouteBuilder(){
    public void configure(){
      fromF("stomp:queue:test?brokerURL=tcp://localhost:%d",getPort()).transform(body().convertToString()).to("mock:result");
    }
  }
;
}
