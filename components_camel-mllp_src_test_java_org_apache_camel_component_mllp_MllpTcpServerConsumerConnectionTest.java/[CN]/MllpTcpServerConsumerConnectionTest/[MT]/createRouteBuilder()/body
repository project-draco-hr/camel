{
  return new RouteBuilder(){
    String routeId="mllp-receiver";
    public void configure(){
      fromF("mllp://%s:%d?autoAck=false",mllpClient.getMllpHost(),mllpClient.getMllpPort()).log(LoggingLevel.INFO,routeId,"Receiving: ${body}").to(result);
    }
  }
;
}
