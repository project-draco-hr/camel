{
  cmisServer=new Server(9090);
  WebAppContext openCmisServerApi=new WebAppContext(OPEN_CMIS_SERVER_WAR_PATH,"/chemistry-opencmis-server-inmemory");
  cmisServer.addHandler(openCmisServerApi);
  cmisServer.start();
}
