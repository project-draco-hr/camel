{
  return new RouteBuilder(){
    public void configure(){
      from("file://target/convertertest?noop=true").to("google-drive://drive-files/insert?inBody=content").to("google-drive://drive-files/list").to("mock:result");
    }
  }
;
}
