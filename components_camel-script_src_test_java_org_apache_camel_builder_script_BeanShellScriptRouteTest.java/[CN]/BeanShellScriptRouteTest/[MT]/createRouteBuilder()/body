{
  return new RouteBuilder(){
    public void configure() throws Exception {
      from("direct:start").choice().when(script("beanshell","request.headers['foo'] = 'bar'")).to("mock:result").otherwise().to("mock:unmatched");
    }
  }
;
}
