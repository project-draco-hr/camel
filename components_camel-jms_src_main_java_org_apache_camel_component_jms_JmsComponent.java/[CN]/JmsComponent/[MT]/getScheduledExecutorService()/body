{
  if (scheduledExecutorService == null) {
    scheduledExecutorService=getCamelContext().getExecutorServiceStrategy().newScheduledThreadPool(this,"JmsComponent",DEFAULT_THREADPOOL_SIZE);
  }
  return scheduledExecutorService;
}
