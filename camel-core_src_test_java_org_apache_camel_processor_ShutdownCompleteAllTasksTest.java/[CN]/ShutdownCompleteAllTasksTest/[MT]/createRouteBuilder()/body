{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from(url).routeId("foo").noAutoStartup().shutdownRunningTask(ShutdownRunningTask.CompleteAllTasks).delay(1000).process(new MyProcessor()).to("mock:bar");
    }
  }
;
}
