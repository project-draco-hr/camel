{
  return new RouteBuilder(){
    public void configure(){
      from("direct:foo").toF("stomp:queue:test?brokerURL=ssl://localhost:%d&sslContextParameters=#sslContextParameters",getPort());
    }
  }
;
}
