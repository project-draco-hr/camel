{
  super.doStop();
  log.info("Stopping RabbitMQ consumer");
  if (conn != null) {
    try {
      conn.close();
    }
 catch (    Exception ignored) {
    }
  }
  channel=null;
  conn=null;
  executor.shutdown();
  executor=null;
}
