{
  Exchange exchange=template.request("http://" + getHostName() + ":"+ getPort()+ "/search?throwExceptionOnFailure=false",new Processor(){
    public void process(    Exchange exchange) throws Exception {
    }
  }
);
  assertExchangeFailed(exchange);
}
