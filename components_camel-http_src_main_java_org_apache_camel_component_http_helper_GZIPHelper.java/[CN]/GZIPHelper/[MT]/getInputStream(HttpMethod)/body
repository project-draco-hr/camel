{
  Header header=method.getRequestHeader(CONTENT_ENCODING);
  String contentEncoding=header != null ? header.getValue() : null;
  return getGZIPWrappedInputStream(contentEncoding,method.getResponseBodyAsStream());
}
