{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      Predicate goodWord=body().contains("World");
      from("direct:start").split(body(List.class),new MyAggregationStrategy()).to("mock:split").filter(goodWord).to("mock:filtered").end().end().to("mock:result");
    }
  }
;
}
