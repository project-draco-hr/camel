{
  MockEndpoint mock=getMockEndpoint("mock:result");
  mock.expectedMessageCount(0);
  template.sendBodyAndHeader("newfile:target/filefilter/","This is a file to be filtered",FileComponent.HEADER_FILE_NAME,"skipme.txt");
  mock.setResultWaitTime(2000);
  mock.assertIsSatisfied();
}
