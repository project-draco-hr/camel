{
  errorHandler(noErrorHandler());
  from(SOAP_ENDPOINT_URI).process(new MappingProcessor(BookStoreImpl.class)).bean(new BookStoreImpl(false));
  from(REST_ENDPOINT_URI).process(new MappingProcessor(BookStoreImpl.class)).bean(new BookStoreImpl(true));
}
