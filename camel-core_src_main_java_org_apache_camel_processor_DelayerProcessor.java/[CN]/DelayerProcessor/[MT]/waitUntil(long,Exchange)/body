{
  while (true) {
    long delay=time - currentSystemTime();
    if (delay < 0) {
      return;
    }
 else {
      try {
        Thread.sleep(delay);
      }
 catch (      InterruptedException e) {
        handleSleepInteruptedException(e);
      }
    }
  }
}
