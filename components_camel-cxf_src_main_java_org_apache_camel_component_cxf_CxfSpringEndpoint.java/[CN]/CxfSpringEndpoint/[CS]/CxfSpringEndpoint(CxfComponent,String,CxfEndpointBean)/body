{
  super(cxfComponent,bean.getAddress());
  this.beanId=beanId;
  this.bean=bean;
  setPropertiesBean();
  configurer=new ConfigurerImpl(((SpringCamelContext)getCamelContext()).getApplicationContext());
}
