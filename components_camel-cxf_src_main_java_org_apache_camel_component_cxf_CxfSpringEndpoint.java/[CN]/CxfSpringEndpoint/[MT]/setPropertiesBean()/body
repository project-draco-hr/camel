{
  if (bean.getProperties() != null) {
    Map<String,Object> copy=new HashMap<String,Object>();
    copy.putAll(bean.getProperties());
    IntrospectionSupport.setProperties(getCamelContext().getTypeConverter(),this,copy);
  }
}
