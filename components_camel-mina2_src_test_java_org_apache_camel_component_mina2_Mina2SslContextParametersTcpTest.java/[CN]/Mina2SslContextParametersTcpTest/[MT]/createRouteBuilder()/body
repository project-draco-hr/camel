{
  return new RouteBuilder(){
    public void configure(){
      fromF("mina2:tcp://localhost:%1$s?sync=false&minaLogger=true&sslContextParameters=#sslContextParameters",getPort()).to("log:before?showAll=true").to("mock:result").to("log:after?showAll=true");
    }
  }
;
}
