{
  return new RouteBuilder(){
    public void configure(){
      interceptFrom().choice().when(header("foo").isEqualTo("bar")).to("mock:b").otherwise().proceed();
      from("direct:start").to("mock:a");
    }
  }
;
}
