{
  if (streamCache != null) {
    streamCache.reset();
  }
  if (readCache != null) {
    readCache.reset();
  }
  if (stream != null) {
    try {
      stream.reset();
    }
 catch (    IOException e) {
      throw new RuntimeCamelException(e);
    }
  }
}
